# ASTRA — Evidence

**Living document.** One row per claim the project makes, the run that produced
it, the command that reproduces it, and the date. A number that is not in this
table has not been measured, and a number here that cannot be reproduced by its
command is a defect in this table.

**Last verified** 11 August 2026, on WSL2 Ubuntu / CPython 3.12.13 / CPU only.

> ### What a date in this table means, and the correction of 9 August 2026
>
> **The rule, from 9 August:** a row's stamp is the date the row *landed in the
> repository*, recovered with
> `git log --reverse --date=short -- docs/EVIDENCE.md`. Not the date someone
> remembers running it. A landing date is a hard fact anyone can re-derive and a
> hard upper bound on when the measurement could have been taken; a remembered
> date is neither.
>
> **Every row in this table was stamped `2 Aug` until 9 August, and for most of
> them that was wrong.** The convention was set when the file was prepared and
> then carried forward unchanged through four more days of work. Corrected:
> E-1 – E-23 to **5 Aug**, E-24 – E-41 to **6 Aug**, E-42 – E-43 to **9 Aug**.
>
> This is recorded rather than quietly fixed because it is the same defect class
> as OD-2 and OD-7 — a document asserting something no code checks — and because
> the rows after E-23 were not merely unverified but *provably impossible*: E-24
> measures the fail-safe speed cap fixed in `17a85c3` on the 6th, and E-33
> measures the per-context twin heads added in `a0aee5c` on the 6th. Neither
> could have been measured on the 2nd, because neither existed. The header
> compounded it: it cited `5a539d0` as the verification commit while dating the
> verification three days before that commit was authored.
>
> **What remains uncertain, stated rather than papered over.** For E-1 – E-23 the
> repository cannot distinguish *"run on the 2nd, committed on the 5th"* from
> *"the stamp was already wrong."* Both are consistent with the history. The
> landing date is used because it is the one of the two that is checkable.

> ### Commit hashes moved on 9 August 2026
>
> Thirty-five unpushed commits were re-authored from `Your Tanay S` — a
> half-overwritten `git config user.name`, never a second person — to
> `Sushanth C`, so every hash after the branch point changed. Every citation in
> this document, the matrix and the 9 August handover was remapped by commit
> subject and is correct as it stands.
>
> Two things are deliberately **not** remapped, and are noted here rather than
> left to be discovered:
>
> - **Hashes quoted inside commit messages are stale.** A message cannot cite a
>   hash that depends on that message. `backup-before-reauthor-20260809` tags the
>   pre-rewrite tip, so any of them can still be resolved.
> - **Eight commits before `d99150f` keep the wrong author.** They are already
>   published on the shared remote, and rewriting them would mean force-pushing
>   over a branch this repository does not own. Left as a decision for whoever
>   owns that call, not taken quietly.

> ### A 200 MB blob was stripped from history on 10 August 2026
>
> `docs/PENDING.md` was 63 KB, and a `str.replace` accident on 6 August made it
> **210 MB** in two commits before it was repaired in the third. The repair fixed
> the *file*; the blob stayed in the *history*, and GitHub refuses any pushed
> object over 100 MB. **The branch was therefore unpushable from 6 August, and
> nothing found out until the first push was attempted on the 10th** -- four days
> and thirty-odd commits later.
>
> The two commits were rewritten to carry their parent's copy of the file, so
> they no longer touch `PENDING.md` at all and the repair two commits later
> reads as an ordinary edit. Nothing else in any commit changed, and the working
> tree is byte-identical. Twenty-six hashes moved and four citations were
> remapped; `backup-before-blob-strip-20260810` tags the pre-strip tip.
>
> `make check` now runs `make blobsize`, which fails on any tracked file over
> 5 MB. A limit set just under the one that bites gives no warning; this one
> complains while the mistake is still a working-tree change rather than four
> days of history.

> **The caveat that governs every row in the first table.** The plant, the digital
> twin and the calibration corpus all descend from the same kinematic bicycle
> model. The generator and the judge agree by construction, so **no
> false-positive or false-negative rate exists and none can exist** until the
> CARLA work in Phase 7. Everything below demonstrates that the machinery works.
> None of it demonstrates that the gates catch what they claim to.

---

## Reproducing everything in this document

```bash
# in WSL2 -- the gate does not run on the Windows host (Smart App Control)
uv sync --all-groups --all-extras --python 3.12
uv run python -m compileall -q .venv/lib/python3.12/site-packages
make check
uv run python benchmarks/latency.py
uv run python -m benchmarks.soak --ticks 100000 --window 1000
uv run python -m benchmarks.flake_hunt   # ~40 min under load
uv run python -m benchmarks.fault_study
uv run python -m benchmarks.comparison
uv run python -m benchmarks.ablation
uv run python -m benchmarks.effectiveness
uv run python -m benchmarks.platform_transfer
uv run python -m benchmarks.parity
uv run python -m benchmarks.commissioning
uv run python -m benchmarks.redundancy
uv run astra explain var/soak/<name>/audit 250
make verify-install
```

Artefacts land in `var/soak/<name>/` — `windows.jsonl` (one row per 1,000 ticks),
`summary.json`, `soak.png`, and the full `audit/` evidence log. The studies write
`var/faults/`, `var/comparison/`, `var/ablation/`, `var/effectiveness/`,
`var/platform/` and `var/commissioning/`.

`platform_transfer` **exits non-zero if any platform HALTs or stops**, so OD-12
regressing is a failed command rather than a table someone has to re-read.

---

## Measured

| # | Claim | Value | Produced by | Date |
|:--:|---|---|---|:--:|
| E-1 | Quality gate is green | **3,042 tests + 3 strict xfail**, `ruff` + `mypy --strict` over **166** files + **12** import contracts, 0 broken | `make check` | 15 Aug |
| E-2 | The closed loop is stable over a long run | **All ten soak criteria pass over 100,000 ticks** | `python -m benchmarks.soak --ticks 100000 --window 1000` | 5 Aug |
| E-3 | A command is issued on every tick | **100,000 of 100,000**, and 400,000 of 400,000 across the four runs of the day | as E-2 | 5 Aug |
| E-4 | The proposer's commands are accepted | `PROPOSED` on **99,997** ticks; **3** vetoed, all answered by the rate limiter. Veto rate 3×10⁻⁵, which the console rounds to 0.0% — the JSON is authoritative | as E-2, `summary.json` | 5 Aug |
| E-5 | Lane keeping | mean \|deviation\| **0.0332 m** over the whole run; 0.0331 across warm windows, drift +0.0001 m | as E-2, `summary.json` | 5 Aug |
| E-6 | The fail-safe machine stays nominal | Never leaves `NOMINAL` across 100,000 ticks | as E-2 | 5 Aug |
| E-7 | No unbounded growth in any rolling structure | Resident set **+0.2 MiB** peak over 100,000 ticks | as E-2 | 5 Aug |
| E-8 | Per-tick cost does not grow | Full-pipeline p99 **9.50 → 9.32 ms** between halves (×0.98) | as E-2 | 5 Aug |
| E-9 | Evidence archive is complete | **0** audit records dropped by the sink's bounded queue across 100,000 records | as E-2 | 5 Aug |
| E-10 | Hot-path software latency | L1+L2+L7a+L8 combined p99 **0.811 ms** against a 10 ms budget; every stage within budget | `python benchmarks/latency.py` | 5 Aug |
| E-11 | No command is issued under a blocking verdict | **0**, from 99,808 per 100,000 before ADR-0016 | as E-2, `proposals_issued_under_veto` in `windows.jsonl` | 5 Aug |
| E-12 | Conformal coverage is arithmetically correct | Per class, shuffled split: **94.9–95.1%** against a 95% nominal, mean over 200 splits | `python -m training.generate_calibration --policy var/policy/synthetic.pt` | 5 Aug |
| E-13 | The statistical gate discriminates between proposers | Matched proposer vetoed on **1 tick in 300**; the placeholder, off-distribution, on **299** | `pytest tests/integration/test_closed_loop_policy.py` | 5 Aug |
| E-14 | The trained policy holds speed and lane in its own environment | Return **936.8/1000**, mean \|deviation\| **0.0913 m**, mean \|long. accel\| **0.0502 m/s²**, all three constraints satisfied | `python -m training.train_policy` | 5 Aug |
| E-15 | The proposer respects the jerk bound far better after rescaling | Peak lateral jerk **150 → 60 → 16.7 m/s³** across three retrains | as E-14 | 5 Aug |
| E-16 | The L1 atomicity test detects the defect it names | Hoisting the clock read out of the lock fails it **6 runs of 6**; the other **2,543** tests all pass | mutation, recorded in `tests/unit/test_l1_sensor_bus.py` | 5 Aug |
| E-17 | CARLA installs and imports on the project's interpreter (assumption A-8) | `carla==0.9.16` installs on **CPython 3.12.13**/Linux with no dependency tree; `Client`, `World`, `Vehicle`, `Sensor`, `Transform`, `Location`, `Rotation`, `VehicleControl`, `WorldSettings` all present; a `Client` constructs and fails to connect with `RuntimeError`, correctly, with no server running | `uv venv --python 3.12 ~/carlacheck && uv pip install --python ~/carlacheck/bin/python carla==0.9.16` | 5 Aug |
| E-22 | The closed loop is stable **under sensor noise** | All ten criteria over 100,000 ticks with readings drawn at the declared sigmas: `PROPOSED` on **99,911** ticks, `RATE_LIMITED` on 89, lane deviation **0.0290 → 0.0298 m**, fail-safe never leaving NOMINAL, resident +0.1 MiB, p99 ×0.81 | `python -m benchmarks.soak --ticks 100000 --window 1000` | 5 Aug |
| E-23 | The Trust Index is a graded index, not a flag | Distinct values across 8,001 ticks: **2 → 5 → 90**. Mean 0.960, p05 0.902, p50 0.964, p95 1.000 | as E-22, `trust.trust_index` in the audit log | 5 Aug |
| E-18 | The Trust Index and the gate were calibrated against incompatible statistics | Gate scores span **1.155–1.191**; innovations span **0.518–7.497** (clear) and **7.549–154.8** (degraded). Sharing one distribution pinned the Trust Index to **2 distinct values across 4,001 ticks**. Separating them gave 5; matching the filter's `R` to the injected noise on top gave 90 — see E-23 | `python -m training.generate_calibration --policy var/policy/synthetic.pt`, then read `innovations` in the corpus | 5 Aug |
| E-26 | The L1 concurrency flake does not reproduce under heavier load than the one that found it | **220 runs, 220 passes, no hangs**: 20 full-suite runs (76.8-100.2 s each) and 200 runs of the threaded tests alone (2.0-4.8 s each), under `stress-ng --cpu 32` on 16 cores. Absence of evidence, not evidence of absence -- a 1%-per-run race survives 220 runs about one time in nine | `python -m benchmarks.flake_hunt --repeats 20 --focus-repeats 200`, `var/flake/p22-campaign/summary.json` | 6 Aug |
| E-27 | The frozen-install check detects the defect it names | Adding `import numpy` to `src/astra/kernel/units.py` makes `make verify-install` fail with `ModuleNotFoundError` and exit 1. Without it, a bare venv holding only pydantic imports the kernel and contracts cleanly | `make verify-install` | 6 Aug |
| E-28† | The configured EWC penalty did nothing at all | At `ewc_lambda` 0 and 150, forgetting after 4,000 samples of a second context was **0.038972** and **0.038951** -- identical to four decimals. Cause was the anchor, re-taken every buffer flush, not the value; see ADR-0018 | superseded -- see † | 6 Aug |
| E-29† | With the anchor held across a context, EWC responds monotonically | Forgetting relative to unregularised: **0.86** at 1e2, **0.30** at 1e3, **0.081** at 1e4, **0.026** at 1e6, **0.018** at 1e8 -- no cliff over six orders of magnitude. Before the fix the only useful value was 1e6 and 3e6 was *worse than zero* (2.33x) | superseded -- see † | 6 Aug |
| E-30† | Catastrophic forgetting is real in this twin | An offline-trained twin knows the highway to **0.000247** mean absolute command error; 4,000 unregularised samples of a second context take it to **0.039219**, a **159x** degradation | superseded -- see † | 6 Aug |
| E-33 | Per-context heads make forgetting exactly zero | Adapting 4,000 rain samples into the rain head leaves the highway prediction **bit-for-bit unchanged** -- the assertion is `==`, not a tolerance. The same rain driven through the *highway* head still destroys it | `pytest tests/unit/test_l5_forgetting.py` | 6 Aug |
| E-34 | Separating the heads costs nothing in adaptation | The rain head closes at least as much of the context gap as a shared head does, because it sees the same gradients. The elastic penalty it replaced gave up a proportional unit of plasticity for every unit of retention (E-32) | as E-33 | 6 Aug |
| E-36 | Running FB2 in shadow changes nothing about the run | 100,000 ticks with `--shadow-fb2` are numerically identical to the run without it: same lane-deviation trace **0.0290 -> 0.0298 m**, same origin split **99,911 PROPOSED / 89 RATE_LIMITED**, all ten criteria pass, live twin digest constant | `python -m benchmarks.soak --ticks 100000 --window 1000 --shadow-fb2` | 6 Aug |
| E-37 | FB2 drifts the twin substantially even with nothing to adapt to | In a **single unchanging context**, twin divergence **0.0094 -> 0.2360** over 100,000 ticks, peak 0.2387, 100 distinct shadow digests. Growth per quarter +0.0622, +0.0627, +0.0542, +0.0474 -- decelerating ~0.87x/quarter, extrapolating to ~0.5, comparable to the whole steering range. Not converged after 83 minutes of simulated driving | as E-36, `mean_shadow_divergence` in `windows.jsonl` | 6 Aug |
| E-38 | FB2's only training labels are the proposer's commands | `_consolidate`'s data term is `MSE(predicted, applied)` where `applied` is the issued command. `train_twin.py` by contrast labels from physics, `steer = lateral / gain`. `twin.py`'s module docstring names training the twin on the proposer as the way to disarm the statistical gate | source, `src/astra/layers/l5_twin/twin.py` and `training/train_twin.py` | 6 Aug |
| E-39 | FB2 would disarm the statistical gate | Over 100,000 ticks in one unchanging context the score against the **live** twin is flat -- **1.1564 -> 1.1560** -- while the score against a shadow twin FB2 adapted falls **1.1534 -> 0.6962**, a **40% collapse**, monotone and still falling. Same run, same ticks; the only difference is which twin the score is computed against. Against the corpus's HIGHWAY_CLEAR quantile of **1.179**, headroom to a veto goes from **x1.02** to **x1.69** -- an anomaly would have to be 69% above the twin's prediction to trip the gate, against 2% today | `python -m benchmarks.soak --ticks 100000 --window 1000 --shadow-fb2`, `mean_live_score` / `mean_shadow_score` in `windows.jsonl` | 6 Aug |
| E-40 | Online requantilisation drives the veto rate to epsilon, by construction | FB3 in shadow over 100,000 ticks: quantile **1.1720 -> 1.1575**, converged within 2 windows and stable thereafter; veto rate it would have produced **5.02%** steady, against **0.089%** today. 5.02% is `significance_epsilon` = 0.05 -- calibrate on the distribution you are then tested against and epsilon of it exceeds the 1-epsilon quantile whether or not anything is wrong | `python -m benchmarks.soak --ticks 100000 --window 1000 --shadow-fb2`, `var/soak/p32-fb3/windows.jsonl` | 6 Aug |
| E-41 | The live loop is not exchangeable with its calibration corpus | Live non-conformity scores sit at **1.156**, below the corpus **minimum** of 1.158; the whole HIGHWAY_CLEAR distribution spans **1.158-1.189** over 1,000 samples. Today's 0.089% veto rate is that mismatch, not evidence the gate is discriminating. E-12's coverage was measured on shuffled splits *of the corpus* and does not transfer | read `var/calibration/synthetic.json`; `mean_live_score` in `var/soak/p32-fb3/windows.jsonl` | 6 Aug |
| E-42 | Per-tick and per-intervention false-positive rates differ by ~600x | At the gate's **designed** operating point (calibration matched to the live distribution, so the per-tick veto rate is epsilon), 100,000 ticks give **4.97%** of ticks vetoed but only **8 ticks outside NOMINAL -- 0.008%**. Two episodes in 83 minutes of driving, 2 and 6 ticks, both self-recovering; **LIMP and HALT were never reached**. A veto runs the fallback for one tick, and the OOD counter must cross theta-1 = 10 before the posture degrades | `python -m benchmarks.soak --ticks 100000 --window 1000 --shadow-fb2`, `shadow_failsafe_states` in `var/soak/d1-intervention/windows.jsonl` | 9 Aug |
| E-43 | The control effectiveness is exactly recoverable from measured response | `a_lat / steer` recovers the configured **B = 140.0** to **0.000%** error, sigma 0.0000, over 3,824 samples -- **provided saturated samples are excluded**. Admitting them reads **116.0**, 17.1% low, and low is the dangerous direction: an underestimated B shrinks the departure the non-conformity score is computed from. The plant clamps lateral acceleration at 3.0 m/s^2, so it saturates beyond \|steer\| = 0.0214 | ADR-0020; `training/environment.py` driven directly | 9 Aug |
| E-24 | The fail-safe speed cap constrains an actuator | Driving the **assembled** pipeline into HALT (whose cap is 0.0 m/s) at 45 m/s: every tick in HALT issues throttle **0.0**, brake **1.0**, origin `SPEED_CAPPED`. A nominal drive issues nothing so labelled. Before P2.1 the identical situation issued a bit-identical command to the uncapped path — see finding F2 | `pytest tests/integration/test_full_pipeline.py -k halt_actually_brakes` | 6 Aug |
| E-44 | The fault injector injects what it claims, and nothing else | Each kind changes the channel it names by the amount it names, on exactly the ticks it names, and leaves every other reading identical. `FaultEpisode` reports the peak error **measured while injecting** rather than the magnitude requested, so an injector that had become a no-op reports a zero peak against an unchanged specification. A `FaultSpec` that could not inject -- zero magnitude, empty window, a magnitude on a kind with no use for one -- raises at construction | `pytest tests/unit/test_fault_injection.py` (27 tests) | 9 Aug |
| E-45 | A fault-free injector is bit-identical to no injector | Whole-trace equality, not a tolerance: same lane-deviation series, same speed series, same issue decisions, same veto count. The injector draws from a stream seeded disjointly from the harness's noise and draws **nothing** on a tick where no window is open. This is what makes the fault the only difference between two arms of a comparison | `pytest tests/integration/test_closed_loop_faults.py -k are_the_same_run` | 9 Aug |
| E-46 | **Ten seconds of frozen IMU puts the vehicle 4.199 m off a 1.75 m lane, and Core-B's verdict trace is identical to the control's** | 400 ticks, same seed, same policy, one arm with a 200-tick IMU dropout. Final lane deviation **0.009 m → 4.199 m**; truth outside the ±1.75 m corridor on **73 ticks**. Vetoes **3 → 3**, reason codes identical (`PHYSICAL:LATERAL_JERK_EXCEEDS_LIMIT` ×3, all from the startup transient). Fail-safe **NOMINAL on all 400 ticks** in both arms. L1 detects the fault correctly -- IMU `DEGRADED` from the second tick of the window -- and nothing downstream acts on it | `python -m benchmarks.fault_study` | 9 Aug |
| E-47 | A slow position drift is equally invisible | 2.0 m of drift ramped over 200 ticks -- 1 cm per tick, so no per-tick delta is anomalous. Final deviation **2.025 m**, truth outside the corridor on **34 ticks**, vetoes **3 → 3**, reason codes identical, fail-safe NOMINAL throughout | as E-46 | 9 Aug |
| E-48 | **The mechanism: the estimate never leaves the corridor while the vehicle does** | Max \|estimate\| against max \|truth\| over the fault window: dropout **0.023 m vs 4.199 m**; drift **0.235 m vs 2.025 m**; bias **1.146 m vs 1.710 m**. The estimate is outside ±1.75 m on **0 ticks in all three**. The proposer closes the loop on the corrupted estimate and drives *it* to zero, and L7a's corridor bound reads the same estimate — so the bound P2.1a added specifically to catch a lane departure cannot fire on a departure a sensor fault caused, by construction | as E-46 | 9 Aug |
| E-49 | Availability holds under every injected fault, and says nothing about safety | **400 of 400** ticks issued a command in all six scenarios and the control. Quoted alone this is the flattering half of E-46: a command was issued on every tick while the vehicle drove two and a half lane widths out of its corridor | as E-46 | 9 Aug |
| E-50 | Two of the six scenarios did not stress what they named, and are reported as null | `speed_bias` injected +3.0 m/s against L7a's configured limit of **120 km/h = 33.3 m/s**, so the corrupted reading of ~16 m/s never approached the bound. `speed_stuck` froze a channel that barely moves, giving a peak divergence of **0.078 m/s**. Both left the vehicle where the control left it. Recorded because six rows presented as six tests would overstate the coverage by two | as E-46 | 9 Aug |
| E-51 | **P2.7 measured: of three candidate detectors, one catches the worst fault and none catches the slow one** | Three shadow detectors, each a pure function of a `DecisionRecord`, changing no verdict. Latency from the fault opening: **health** fires on `imu_dropout` at **+5 ticks** and is silent on all five others; **innovation** fires only on `lateral_noise`, at **+84 ticks**; **trust** fires on `imu_dropout` at **+5** and `lateral_noise` at **+10**. **Zero false alarms on the control for all three.** `position_drift` — the 2.025 m departure — is silent on all three | `python -m benchmarks.fault_study` | 9 Aug |
| E-52 | Gating on stream health would catch the dropout with 68 ticks to spare | The health detector fires at tick **+5** of the fault window; the vehicle first crosses the ±1.75 m corridor at **+73**. **3.4 seconds of margin** at 20 Hz, against a fault that currently produces no reaction at all. The signal is already computed by L1 and already recorded; nothing in Core-B reads it | as E-51 | 9 Aug |
| E-53 | **P2.7 option B is refuted by measurement, not by argument** | The innovation sequence does **not** detect a slow position drift. Ramping 2 m over 200 ticks is 1 cm per tick against a declared sigma of 0.1 m, so every step is well inside what the filter expects and the innovation never leaves its band. It was the principled candidate — the one recorded quantity that can disagree with the estimate — and it is silent on the fault it was most wanted for | as E-51 | 9 Aug |
| E-54 | The innovation reaches the evidence log at all | Audit schema **v2 → v3** adds `fast_innovation` to every decision record. The quantity was always computed — L6's covariate-shift window is fed from it and L3's Trust Index is derived from it — and reached the archive through neither, so a run's evidence did not contain the one number in the pipeline that can disagree with the state estimate. Found while trying to answer *"could anything in Core-B have seen OD-9?"* from the archive, and discovering the archive could not say | `pytest tests/unit/test_contracts_audit.py tests/unit/test_constants.py` | 9 Aug |
| E-55 | The detectors are verified to fire, so their silence means something | 17 tests: each fires on the condition its docstring names, ignores the condition it does not, enforces `PATIENCE` consecutively rather than cumulatively, and treats a missing field as *no evidence* rather than evidence of trouble. Without this, a detector broken into permanent silence would have produced E-51's table unchanged and it would have read as *"nothing could have seen it"* | `pytest tests/unit/test_detectors.py` | 9 Aug |
| E-56 | **P3.5: ASTRA and an ungated baseline both leave the corridor, and on one fault ASTRA is worse** | Two arms, same seed, same policy, same plant, bit-identical corrupted readings. Final \|deviation\| / ticks outside the ±1.75 m corridor — **ASTRA vs Core-A raw**: control **0.009 m/0** vs 0.055 m/0; `imu_dropout` **4.199 m/73** vs **1.707 m/0**; `position_drift` 2.025 m/34 vs 2.001 m/27; `position_bias` 0.931 m/0 vs 0.960 m/5; the three harmless faults agree to within 0.1 m. Both arms issue on every tick; neither ever stops | `python -m benchmarks.comparison` | 9 Aug |
| E-57 | The baseline's advantage under dropout is an artefact of what it is handed, not of having no filter | Core-A raw is given the plant's **true heading**, because nothing in this plant publishes one and the policy's observation requires it. On the control run that generosity leaves it **worse** than ASTRA — 0.055 m against 0.009 m — so it does not make the baseline better in general. It makes it better exactly when the filter is being lied to. **The baseline is not realisable**, and the row measures the filter's failure mode rather than an advantage of removing it | as E-56 | 9 Aug |
| E-58 | **A frozen position sensor corrupts the *unobserved* heading estimate fortyfold** | Under the 200-tick dropout, true heading reaches **0.0686 rad** while the UKF reports **0.0017 rad**; mean \|error\| **0.0334 rad** against 0.0140 clean. A frozen reading is maximally self-consistent, so the filter grows confident in it, and keeping *"y is not changing"* consistent with the motion model forces the conclusion that the vehicle is not turning. The error is pushed into the one state nothing measures. **The fault does not stay in the channel it was injected into** — proposer and all three gates then read the result. Predicted as heading *noise* and measured as heading *suppression*: the estimate's range narrows to ±0.026 rad from ±0.056 clean | as E-56, with heading truth read from the plant | 9 Aug |
| E-59 | **P3.4: L7b produces essentially every veto; L7a produces none** | Four profiles x seven scenarios, 2,800 ticks each, same seed. Vetoes per run — governed / L6 off / L7b off / L7a off: control **3/3/0/3**; `imu_dropout` **3/3/0/3**; `position_bias` **12/11/1/12**; every other scenario **3/3/0/3** or **4/4/0/4**. Disarming **L7b takes the veto count to zero on six of seven scenarios**. **L6 contributes one veto in 2,800 ticks**, on `position_bias` alone. **L7a contributes exactly zero, everywhere** | `python -m benchmarks.ablation` | 9 Aug |
| E-60 | Disarming any single gate moves the vehicle by at most 5 mm | Final \|deviation\| is identical to three decimals across all four profiles on five of seven scenarios; the largest difference anywhere is `imu_dropout` under L7b off, **4.204 m against 4.199 m**. Every run issued a command on all 400 ticks. On these scenarios the gates change *what is recorded*, not *where the vehicle goes* | as E-59 | 9 Aug |
| E-61 | The ablation demonstrably happened | Positive control: disarmed gates still run and still write a verdict, counted as `ablated_passes`. **0 of 2,800** in every governed row; **2,800 of 2,800** in every disarmed row. A study that silently failed to ablate would report zero everywhere and would otherwise look identical | as E-59, `ablated_passes` in `var/ablation/summary.json` | 9 Aug |
| E-62 | A pipeline with no gate stays unconstructible | ADR-0021 implemented: `statistical_gate`, `physical_gate` and `shield` remain **required, non-optional** constructor parameters, and an ablation supplies a subtype that runs and cannot block. An architecture test reads the constructor signature and fails if any of the three is ever widened to admit `None`. Audit schema **v3 → v4**: every record carries which layers were disarmed, so an ablated run is self-identifying in its own evidence and can never be mistaken for a governed one | `pytest tests/unit/test_ablation.py` (33 tests) | 9 Aug |
| E-63 | **ADR-0020's estimator, placed as specified, reports the configuration back to itself** | Fed the filtered estimate, it returns **140.000 on every platform**, including plants whose true `B` is **112.0** and **168.0**. Structural, not a tuning fault: the UKF's process model already assumes `B`, so its lateral-acceleration estimate is that assumption propagated. Wired this way the loop would report *"the platform has not changed"* for any platform | `python -m benchmarks.effectiveness` | 9 Aug |
| E-64 | Fed the raw measured response instead, it tracks the platform | **111.341** against a true 112.0, **165.140** against a true 168.0 — within 1.7%. Residual **~1.5% low** on the control run, unexplained, and in the direction ADR-0020 names as dangerous: an underestimated `B` shrinks the departure the non-conformity score is computed from | as E-63 | 9 Aug |
| E-65 | No injected fault moves the effectiveness estimate materially | Across six faults the widest deviation from the control is **0.4%** (137.463 against 137.843); the 25x-sigma noise burst leaves it at **140.000** exactly, because the estimate is a median over 200 samples. The OD-9 risk this shadow run was written to look for — a sensor fault rewriting a platform constant shared by the twin, L7b and the projector — **did not materialise on these scenarios** | as E-63 | 9 Aug |
| E-66 | **The evidence log is tamper-evident** | Every record carries the SHA-256 digest of the serialised line before it; the first carries a fixed genesis value, so a verifier needs nothing but the file. Altering, removing, inserting or reordering a record anywhere breaks verification — including the realistic attempt, where a record is altered *and* its own link repaired, because the successor's link covers the altered line's digest. Audit schema **v4 → v5** | `pytest tests/unit/test_audit_chain.py` (16 tests) | 10 Aug |
| E-67 | Two things chaining cannot do, asserted rather than omitted | **Truncating the tail is undetectable** — a prefix of a valid chain is a valid chain — and **a consistent rewrite of the whole file is undetectable**, because an adversary who can rewrite every record can recompute every link. Both are asserted as tests rather than left to prose. Closing either needs an independently held digest of the final record, or a signed root, and signing needs key management this project does not have | as E-66 | 10 Aug |
| E-68 | **FilterPy is gone, and every recorded number reproduces** | The UKF is now `astra.layers.l2_estimation.unscented`, ~250 lines, matching FilterPy's algorithm rather than improving it. Re-run after the swap: the fault study reproduces **every** figure of E-46, E-49 and E-50 to three decimals — 0.009 / 4.199 / 0.931 / 2.025 / 0.008 / 0.075 / 0.140 m and vetoes 3 / 3 / 12 / 3 / 3 / 3 / 4 — the detector table reproduces E-51 exactly, and the comparison harness reproduces E-56 exactly. `scipy`, `matplotlib`, `pillow` and `filterpy` all leave the runtime tree; nothing in `src/` ever imported the first three | `python -m benchmarks.fault_study`, `python -m benchmarks.comparison` | 10 Aug |
| E-69 | The replacement agrees with what it replaced to 6e-10, and cannot do better | Over 2,000 predict/update steps with varying measurement dimension: state **6.0e-10**, covariance **1.1e-14**, Mahalanobis **2.2e-09**. Sigma points and weights are **bit-identical**. The residual is LAPACK — FilterPy factors covariance with SciPy's upper-triangular Cholesky, this uses NumPy's lower-triangular one — and removing it would mean keeping SciPy, which is the point of the exercise. E-68 is the check that matters: the difference changes no decision | comparison probe, recorded in `tests/unit/test_unscented.py` | 10 Aug |
| E-70† | **The innovation covariance omits the process-noise term** | The update reuses the sigma points `predict` pushed through `fx`, whose spread is `F P Fᵀ` — without the `Q` that predict added analytically — so `S` is short by exactly `H Q Hᵀ`. At the simulation operating point `Q` diag is **0.02 / 0.05 / 0.3** against `R` diag **0.01 / 0.0001 / 0.0016**, giving **per-channel** bounds of 1.73x, 22.4x and 13.7x. Inherited from FilterPy, not introduced by the port, and found by writing the test that checks the port against the textbook Kalman filter. **† The 22.4x is an algebraic bound and was quoted as the effect when this row was first written, which overstated it — see E-71 for what it actually does** | `pytest tests/unit/test_unscented.py -k omits_the_process_noise` | 10 Aug |
| E-71 | **The realised inflation is 1.24x at the median, and 1.02x on the largest innovation** | Both statistics computed side by side over a 400-tick run, correcting nothing. Recorded against corrected, by band of recorded distance: bottom 50% **0.179 → 0.129 (1.53x)**; 50–90% **0.381 → 0.296 (1.41x)**; **top 10% — the tail that drives vetoes — 1.134 → 1.028 (1.23x)**; the single largest distance **20.806 → 20.309 (1.024x)**. The per-channel 22.4x assumes the innovation lands entirely in the speed channel; in the closed loop it is dominated by lateral position, where the factor is 1.73x. **The correction matters least exactly where the statistic is used for decisions** | shadow probe over `drive_closed_loop`, no behaviour changed | 10 Aug |
| E-72 | **NFR5 tested against a warehouse AGV: it holds for the gates and fails for the plumbing** | A differential-drive AGV — two wheels in rad/s, no throttle, no brake, no steering angle, turning on the spot — built as an adapter and driven at the pipeline. Four walls, each verified by execution rather than by reading. **The gates are genuinely neutral**: `FAST_STATE_FIELDS`, the actuation-space contract, the `Policy` protocol and the projector role all accepted the AGV unchanged | `pytest tests/architecture/test_domain_independence.py` (6 passed, 5 strict xfail) | 10 Aug |
| E-73 | **Wall 1 and 2 — the composition root is automotive and not injectable** | `assemble_pipeline` has **14 parameters and none accepts an actuation space**; it calls `automotive_actuation_space()` directly. The command projector is equally fixed, and `AutomotiveCommandProjector` divides a target lateral acceleration by a *steering effectiveness*, arithmetic a differential drive has no counterpart for. The module docstring 300 lines above the call claims *"a different platform supplies a different space and no layer notices"* — **the file contradicts itself** | as E-72 | 10 Aug |
| E-74 | **Wall 3 — L2's process model is a bicycle model, and this one is not cosmetic** | `fast_transition` derives yaw rate from `a_lat / v` and takes a `yaw_rate_minimum_speed` below which it refuses. Driven with the AGV's ordinary condition — zero forward speed, non-zero lateral acceleration, i.e. pivoting into a rack aisle — the propagated heading is **exactly 0.000000**: the model says the platform cannot turn. This is domain knowledge **inside a layer**, which is what NFR5 forbids, and unlike the other three it cannot be fixed by moving a symbol | as E-72 | 10 Aug |
| E-75 | Wall 4 — the kernel names automotive quantities | `SLOW_STATE_FIELDS` is `road_friction_coefficient`, `tyre_wear_index`, `sensor_health_score`; `ContextClass` is `HIGHWAY_CLEAR`, `URBAN_CLEAR`, `RAIN_NIGHT`. Both live in `astra.kernel` — the layer with no dependencies and the strongest claim to neutrality. Cosmetic in name and **operational in effect**: an AGV matches no certified class, so every tick is `UNCLASSIFIED` and bounded safe exploration would engage permanently | as E-72 | 10 Aug |
| E-76 | **The blindness is conditional on the fault continuing** | Same 800-tick run, same seed, same IMU dropout opening at t200 — the only difference is when it *closes*. **Persisting to the end:** 3 vetoes (the startup transient), **no veto ever attributable to the fault**, fail-safe `NOMINAL` on all 800 ticks, final deviation **35.705 m** at 10.9 m/s. **Closing at t600:** 203 vetoes, **first at t600 — the exact tick the sensor recovered** — fail-safe escalating NOMINAL → DEGRADED (20) → LIMP (70) → HALT (101), braking to 0.030 m/s. The graduated response works exactly as designed; it cannot start until the lie stops | `python -m demo.dashboard`, then the dropout button; measured directly with `drive_closed_loop` | 10 Aug |
| E-77 | The departure grows without bound while the fault persists | **4.199 m** over a 200-tick dropout (E-46) against **35.705 m** over 600. Nothing arrests it, because nothing has noticed | as E-76 | 10 Aug |
| E-78 | The dashboard invents nothing | `Frame` is a pure projection of one `TickSample`. Every field is asserted **against the specific record attribute it claims to come from**, and `test_every_field_is_declared_as_record_or_simulator` fails if a field is added without declaring which of the two it is. Exactly three fields are simulator-sourced — `truth_y`, `truth_speed`, `fault_active` — and they are on screen because **OD-9 is invisible without them**: a page showing only what the system knows renders E-46 as a nominal run | `pytest tests/unit/test_dashboard_frame.py` (21 tests) | 10 Aug |
| E-79 | A stage that did not run renders as absent, never as plausible | Asserted per stage. The one that was found by running rather than by testing: L8 imposes **no** speed cap in NOMINAL, and rendering that `None` as `0.0` would have put *commanded stop* — the most alarming reading the panel has — onto every healthy tick. Every fixture had set a cap, so no test covered it | as E-78 | 10 Aug |
| E-80 | **Bounded safe exploration engages on an unrecognised context, and the vehicle keeps moving** | With the cold path live, moving the context signature to a tunnel — visibility **0.05**, road complexity **0.95**, outside every certified centroid — takes RCM from `SHADOW_EXECUTION` to **`SAFE_EXPLORATION`**. The envelope narrows and the effect is visible in the plant: speed **12.5 → 7.39 m/s**, half the nearest certified profile's maximum. **A command still issues on every tick**, all three gates still PASS, and lane deviation holds at **0.027 m**. Nothing instructs RCM to explore — the context moves and RCM decides for itself on its own evaluation period | `python -m demo.dashboard`, then *Enter the tunnel* | 10 Aug |
| E-81 | On a matched context RCM stages a calibration promotion rather than switching | On `(visibility 0.85, traffic 0.7, complexity 0.7)` — tuned to `URBAN_CLEAR`'s centroid **and** to the speed the vehicle actually holds — arbitration reaches **`SHADOW_EXECUTION`** with trust **0.717** against τ **0.70**, running both tables in parallel. It does not commit inside 200 ticks: the divergence index has not cleared, which is the mechanism working rather than stalling | as E-80 | 10 Aug |
| E-82 | The context tuning is measured, because guessing it inverted the result | A context that *looks* like clear highway on the three supplied components — `(0.90, 0.3, 0.2)` — sits in permanent `SAFE_EXPLORATION`, because the signature's second component is **ego speed normalised by the legal limit** and this policy holds 12.5 m/s of 33.3, i.e. **0.375** against `HIGHWAY_CLEAR`'s centroid of **0.8**. Three components matching and one missing is a miss. Had this not been checked, the tunnel scene would have shown exploration against a baseline that was already exploring | as E-80 | 10 Aug |
| E-83 | **Bounded safe exploration halted the vehicle, on two platforms out of five** | The distinguishing claim tested by the cheapest honest means: change the **plant**, leave the twin, corpus and policy fitted to the old one. 600 ticks, seed 20260810, certified context throughout — so the *platform* is what no profile covers. **Weak acceleration (3.0 → 1.5 m/s²): 520 exploring ticks, 352 vetoes, HALT at t398, final speed 0.00 m/s.** **Weak brakes (8.0 → 3.0): 580 exploring, 315 vetoes, HALT at t404, final 0.00.** In both, L8 was counting the same vetoes L9 had already responded to by declaring `SAFE_EXPLORATION` — one condition, two owners, and the terminal answer won (OD-12). The three unchanged platforms explored for 80 ticks and finished NOMINAL, which is why 500,000 ticks of soak had never shown it | `git stash` the three files of ADR-0023, then `uv run python -m benchmarks.platform_transfer` | 11 Aug |
| E-84 | **The exploration envelope's speed cap was enforced against nothing** | Same control arm, and only visible because the run counts `CommandOrigin.SPEED_CAPPED`: before halting, the weak-braking platform reached **23.43 m/s** against the calibrated platform's 14.27, with **zero capped ticks**. `exploration_envelope` computes `speed_cap = nearest_certified_max × 0.5`; `restricted_space` turns the envelope into narrowed *channel* bounds, which limit throttle per tick and bound speed not at all (OD-13). **Fixing E-83 alone would have made this worse** — the halt was the only thing arresting the acceleration | as E-83 | 11 Aug |
| E-85 | **After ADR-0023 every platform finishes NOMINAL and still moving** | Same command, same seed, current tree. Weak acceleration **520 exploring, 306 vetoes, final 4.36 m/s**; weak brakes **600 exploring, 3 vetoes, final 16.58 m/s, \|dev\| 0.237 m**; the three unchanged platforms unmoved from their control values (12.53 / 12.49 / 12.45 m/s, \|dev\| ≤ 0.032 m). No platform reaches HALT. The script exits non-zero if one does, so the regression is a build failure rather than a re-reading | `uv run python -m benchmarks.platform_transfer` | 11 Aug |
| E-86 | **The cap now alters commands, and the record says which ones** | The weak-braking platform is held at **16.72 m/s across 105 `SPEED_CAPPED` ticks** — half the highway profile's 33.34 maximum (16.67), plus one tick of plant integration. The cap flows through the same projector seam P2.1 built for the fail-safe cap, so `SPEED_CAPPED` continues to mean *a command the cap altered* rather than *a cap that existed*. **The 0.05 m/s overshoot is honest and structural**: the cap bounds the command, and the plant integrates it over the tick | as E-85 | 11 Aug |
| E-87 | **A dark sensor now escalates the fail-safe posture, and it beats the hazard** | Same command, same seed, same 200-tick IMU dropout as E-46. **DEGRADED at +5 ticks, LIMP at +15, HALT at +40**, against a corridor departure that begins at **+73** — 33 ticks of margin, 1.65 s at 20 Hz. The escalation is driven entirely by the sensor-integrity counter: the OOD counter never leaves 0, because **no gate vetoes and none ever did**. ADR-0024 | `uv run python -m benchmarks.fault_study` | 11 Aug |
| E-88 | **The measured consequence of OD-9's worst arm falls 25-fold** | Final \|deviation\| under the dropout: **4.199 m → 0.167 m**, inside the ±1.75 m corridor it previously left by two and a half lane widths. Max estimator error **4.210 → 0.178 m**. Still worse than the clean run's 0.009 m, and that is asserted too — the fault is *arrested*, not absent | as E-87 | 11 Aug |
| E-89 | Zero false alarms, and the control is unmoved | The control run holds the integrity counter at **0 across all 400 ticks**, never leaves NOMINAL, and finishes at **0.009 m** — identical to its pre-change value. The mechanism fires on the condition it names and on nothing else | as E-87 | 11 Aug |
| E-90 | **It catches one fault of six, and the other five are unchanged to three decimals** | `position_bias` 0.931 m, `position_drift` 2.025 m, `speed_stuck` 0.008 m, `speed_bias` 0.075 m, `lateral_noise` 0.140 m — every figure identical to the pre-change run, with **peak integrity counter 0** on all five. `StreamHealth` is computed from *staleness*, so a channel publishing a fresh, well-formed, wrong value stays HEALTHY for ever. **This is the honest half of ADR-0024** and it is asserted by a test rather than left to a docstring | as E-87 | 11 Aug |
| E-91 | The response is invisible to ablation, which is a structural-independence argument | Disarming L6, L7a or L7b leaves the dropout's final deviation at **0.167 m** in every profile — because the response does not come from a gate. The ablation study's veto counts are also unchanged: governed 3/3/12/3/3/3/4, `L7b off` 0/0/1/0/0/0/0, exactly as E-59 recorded | `uv run python -m benchmarks.ablation` | 11 Aug |
| E-92 | **The 600-tick dropout: 35.705 m becomes 0.170 m** | E-76's scenario re-run — 800 ticks, dropout opening at t200 and closing at t600. **DEGRADED at t205, LIMP at t215, HALT at t240**, all with `ood_counter` at **0**: not one veto contributed. Final \|deviation\| **0.170 m**, peak **0.369 m**, against E-77's **35.705 m**. At HALT the vehicle is already down to **1.90 m/s** from 12.32. The 203 vetoes still arrive on the tick the sensor recovers, exactly as E-76 recorded — they now arrive at a vehicle that stopped 360 ticks earlier | `uv run python -m benchmarks.fault_study --ticks 800 --open-at 200 --close-at 600` | 11 Aug |
| E-93 | **The same table separates the two causes, by one column** | In the closing-fault run, `imu_dropout` escalates at **+5 / +15 / +40 with peak integrity counter 40**, and `position_drift` escalates at **+410 / +430 / +500 with peak integrity counter 0** — ten ticks after the sensor recovered, driven entirely by vetoes. One is caught at the sensor boundary before the hazard; the other is caught by the gates after the lie stops. **The distinction is legible from the record alone**, which is what two counters buys and one could not | as E-92 | 11 Aug |
| E-94 | **Analytical redundancy is refuted: the parity residual is 2.2x to 4.0x larger than the fault it must detect** | A second state estimate propagated from the *issued commands alone*, never corrected by a sensor, re-anchored to the filtered estimate every W ticks. On a healthy cruising vehicle the residual accumulates at **0.0403 / 0.0272 / 0.0224 / 0.0226 m per tick of window** at W = 10 / 20 / 40 / 100. The slow drift injects **0.0100 m per tick** by construction. **Both grow linearly with the window**, so the ratio is roughly constant and no window separates them. Swept over six windows and three faults: the faulted peak is below the clean peak in every cell but two, and those two are non-monotonic in the window — noise, not signal. Separation 0.20x – 1.61x against a bar of 2x | `uv run python -m benchmarks.parity`; per-arm table in `uv run python -m benchmarks.fault_study` | 11 Aug |
| E-95 | **The reason it cannot work, and it is a mechanism this project relies on** | The two estimates were never independent. **FB1 feeds the issued command into the filter's *prediction* step**, so the filtered estimate and the open-loop propagation share the same process model *and the same command input*; they differ only by the measurement correction. The parity residual is therefore not *commands versus sensors* — it is *how far the measurement pulled the filter*, which under a slow drift is exactly the drift rate. Meanwhile the propagation's own error, heading integrated from `a_lat / v` and position from heading with no correction, accumulates several times faster. **The idea was refuted by the feedback loop that exists to fix the very defect it was built to attack** — and FB1 is not removable, it is the mitigation for the shared-estimate common cause | as E-94 | 11 Aug |
| E-96 | The startup transient was separated before the noise floor was quoted | The plant starts up to 1 m off centre and the correction is a **sustained one-sided manoeuvre — the same shape a fault response has**. Reported together, ticks 0–199 give a peak of 5.411 m at W=100 against cruise's 2.260 m, so quoting the combined figure would have flattered the noise floor by 2.4x by attributing a real manoeuvre to it. Split before any threshold was considered | as E-94 | 11 Aug |
| E-97 | **A commissioning certificate: this platform is certified for one of the four seeded contexts** | Drive the platform through each seeded profile's road conditions in turn and record what RCM does. On the calibrated platform: **`urban_clear` CERTIFIED** (profile held all 400 ticks); **`highway_clear`, `rain_night`, `degraded_sensor` and `tunnel` all BOUNDED** — no profile matched and the vehicle drove anyway, inside the envelope, never halting, peak settled deviation **0.258 m**. **Three of four seeded profiles are unreachable by this platform**, and the certificate says why rather than leaving it to be discovered | `uv run python -m benchmarks.commissioning` | 11 Aug |
| E-98 | **The reason is ego speed, which no deployment can set** | Two of the five signature components are *measured*, not supplied: `ego_speed` from the vehicle and `sensor_reliability` from frame health. This policy realises **0.28** against `HIGHWAY_CLEAR`'s centroid of **0.80**, `RAIN_NIGHT`'s 0.50 and `DEGRADED_SENSOR`'s 0.40 — and matches `URBAN_CLEAR`'s **0.35** almost exactly at 0.35. **A profile whose centroid a platform cannot physically reach is a profile that platform is not certified for**, however well its road conditions match. E-82 recorded this as a tuning trap; the certificate makes it a reported column | as E-97 | 11 Aug |
| E-99 | **The same software on a different vehicle loses its only certified context** | Weak brakes (8.0 → 3.0 m/s²) and worn tyres (B 140 → 112), nothing else changed, same seed: **zero CERTIFIED, five BOUNDED.** `urban_clear` still matches on some ticks but holds for only **40 of 400** — the rest is exploration — and peak settled deviation roughly doubles, **0.255 → 0.565 m**. Neither platform is UNFIT; the certificate distinguishes them anyway, which a pass/fail answer could not | `uv run python -m benchmarks.commissioning --braking-authority 3.0 --steer-effectiveness 112.0` | 11 Aug |
| E-100 | The verdict rule was too lenient on its first run, and the second platform found it | The first version asked only whether a profile had **ever** matched, and reported the weak-braking platform's `urban_clear` as CERTIFIED while it spent **360 of 400 ticks in exploration**. Corrected to require a **majority** — the weakest defensible bar, and deliberately not tuned finer, because a threshold chosen to make a particular platform pass would be fitted to that platform. Pinned by `test_a_profile_that_matched_but_barely_held_is_bounded` | `pytest tests/unit/test_commissioning.py` (13 tests) | 11 Aug |
| E-101 | **OD-14: the arbitration record could not say what context it was about** | `arbitrate()` receives a `RuntimeContextSignature`, searches the knowledge base with it and decides on it — and the `ArbitrationDecision` it returned carried the outcome, the profiles, the trust score and the CDI, and **not the signature**. A record could read `SAFE_EXPLORATION, trust 0.62` and could not say which context produced that. **Third instance of one shape**, after `fast_innovation` (schema 3, E-54) and `previous_digest` (schema 5): a quantity the pipeline computes, consumes, and archives nowhere. Closed by audit schema **6 → 7** | `pytest tests/unit/test_constants.py -k schema_versions`; the field is on every arbitration record from v7 | 11 Aug |
| E-102 | **A calibration request, generated from an evidence log and nothing else** | 600 ticks in the tunnel context. One episode, proposable: centroid **visibility 0.050 ±0.000, ego_speed 0.254 ±0.054, traffic_dynamicity 0.700 ±0.000, sensor_reliability 0.950 ±0.000, road_complexity 0.950 ±0.000**, nearest certified profile `highway_clear@v1`, **0.3% of ticks vetoed, NOMINAL throughout, every tick issued a command**. The vehicle has identified a context nothing covers, evidenced that it handled it safely, and asked for calibration work — **with no path by which it could grant itself any** | `uv run python -m benchmarks.envelope <audit>.jsonl` | 11 Aug |
| E-103 | **The request carries no quantile table, and that is asserted structurally** | `Request` has no `quantile_table`, `coverage_level`, `certified_at` or `expires_at` field, and is not a `CalibrationProfile`. Asserted on the **type** rather than on behaviour, because a proposer that grew a quantile table would still produce correct centroids and spreads and pass every behavioural test in the file. A table fitted to the vehicle's own exploration is FB3 by another name — it pins the veto rate to ε by construction (E-40) | `pytest tests/unit/test_envelope_requests.py` (14 tests) | 11 Aug |
| E-104 | Two contexts in one stretch are refused rather than averaged | A vehicle driving from a tunnel into daylight with no profile matching produces **one** exploration episode whose mean visibility is ≈0.5 — a context that never existed. The coherence filter rejects it by name (`incoherent: visibility spread … > 0.15`) rather than proposing calibration work for a fiction. **Every rejected episode is still reported with its reason**, so an engineer can discover that the filters are wrong | as E-103 | 11 Aug |
| E-105 | **The innovation gate flags the startup transient and nothing else** | `innovation_gate_gamma = 7.5`. Across the control and all six injected faults, the number of ticks exceeding it is **1, 1, 1, 1, 1, 2, 26** — and in every arm including the control **the first flagged tick is tick 0**, the initial 1 m offset the plant starts with, at a distance of 20.806. Only the 25-sigma noise burst moves it at all. So feeding the flag into the sensor-integrity counter would add a **false alarm on tick 0 of every run** and detect nothing. Refuted before it was built | `uv run python -m benchmarks.fault_study`, `fast_innovation` per tick against gamma | 11 Aug |
| E-106 | **Cross-channel consistency catches the bias and is blind to the drift** | Windowed `|mean lateral acceleration|` against `|mean position|`, 40-tick windows, no integration. `position_bias` separates at **4.14x** (0.6177 against a clean 0.1492); **`position_drift` sits at 0.99x** — indistinguishable from a clean run. And the bias detection is weaker than it looks: `|mean position|` was **0.5407** in that window, so the estimate had moved too, and the premise — *sustained turn while the estimate says we are on centre* — is not what occurred | `benchmarks.parity.cross_channel` over the fault study's seven arms | 11 Aug |
| E-107 | **Three independent mechanisms, all silent on the slow drift, for one root reason** | The innovation sequence (E-53), analytical redundancy (E-94) and cross-channel consistency (E-106) have now each been measured against the same fault and each is silent. The root is shared: **a self-consistent lie slower than the sensor noise cannot be distinguished from truth by any function of a single sensor chain.** Every quantity on the record is downstream of the same measurement, and no rearrangement of downstream quantities creates information that was never upstream. **This is the argument for redundancy rather than an assertion of it**, and it is what makes Phase 7 a necessity rather than a preference | as E-105, E-106, and E-53 | 11 Aug | — **RETRACTION WITHDRAWN the same day, see E-143.** E-139 claimed the word *any* was too strong. That claim was measured on a vehicle with 400 of 400 ticks vetoed and a final speed of zero, where this row's own mechanism cannot operate. Re-run on a vehicle that drives, the candidate separates **1.03x**. **This row stands, and now has a fifth confirmation**
| E-108 | **OD-15: the five-modality sensor bus carries one measurement** | `_publish_state` computes **one payload** and publishes it byte-identical to all five modalities; `_Extractor.extract_fast` reads **the IMU only** and discards the other four. So the prototype has five sensor modalities and **one sensor**, and every cross-check that could have caught a lying channel had nothing to check against. **Both facts live in ~30 lines of test harness, not in the architecture**: `FusedSensorFrame` already carries per-modality samples and `MeasurementExtractor` is an injectable port. Nothing in `src/` changed to run E-109 | read `training/closed_loop.py` `_publish_state` and `_Extractor` | 11 Aug |
| E-109 | **Redundancy works, and it is the first candidate that does** | Three dissimilar position channels — IMU sigma 0.1, GPS 0.2, LIDAR 0.06 — with the drift injected into **one**. Residual against the median, faulted peak over clean peak: **drift on IMU 5.3x on IMU** against 1.1x and 2.4x on the others; **drift on GPS 4.4x on GPS** against 0.7x and 1.5x. **Control: 0.8x, 1.1x, 1.3x — nothing separates.** The faulted channel is not merely detected but *identified*, because with three channels the largest residual names the liar | `uv run python -m benchmarks.redundancy` | 11 Aug |
| E-110 | **Detected before the hazard, with margin** | The faulted channel's residual leaves every clean channel's band at **+41 ticks** (IMU) and **+28 ticks** (GPS), against the closed-loop departure that begins at **+73** (E-46). 1.6 s and 2.25 s of margin at 20 Hz. The two numbers come from different arms — the residual measurement drives a clean plant — and are comparable because the drift rate is the same, 0.01 m per tick | as E-109 | 11 Aug |
| E-111 | **The residual is invariant to where the vehicle is, and that is what the other candidates lacked** | `reading_i - median` cancels the truth term exactly, so the statistic measures **sensor disagreement** and nothing else. Verified: readings taken at a true position of 0.0 m and 7.5 m with the same noise draws give residuals equal to **5e-17** — algebraically identical, floating point costing the last two digits. This is precisely what the cross-channel candidate did not have: it measured a *quantity*, so a large correction moved it and it reported the manoeuvre as a fault (E-106) | `pytest tests/unit/test_redundancy.py` (7 tests) | 11 Aug |
| E-112 | The good channels are perturbed too, and the honest margin is 2.2x not 5.3x | Under a drift on the IMU, **LIDAR's residual also rises, to 2.4x**. With three channels the median itself moves once the fault exceeds the noise spread, so the clean channels' residuals are not unaffected. The faulted channel is still the largest — 5.3x against 2.4x, a **2.2x margin** — so identification holds, but the separation to quote is the margin over the *next* channel rather than over the control | as E-109 | 11 Aug |
| E-113 | **The drift that ended 2.025 m out now ends 0.042 m out** | Three dissimilar position channels fused by median, drift injected into the IMU channel at 0.01 m/tick from t200. Final \|deviation\| **0.042 m**. The median rejects the outlier from the first tick, so the lie never reaches the state estimate — **the protection is structural, not a detection**. The clean redundant arm holds **0.050 m** and the single-channel control **0.009 m**: redundancy costs accuracy, because the fused sigma is the worst channel's | `drive_closed_loop(redundant=RedundantSensing.build(...))` | 11 Aug |
| E-114 | **`StreamHealth.FAULTED` is produced for the first time, and it identifies the liar** | The residual monitor calls the drifting channel `FAULTED` at **+84 ticks**, and names *which* channel — with three readings the largest residual identifies rather than merely detects. `FAULTED` had been an unreachable enum member for the project's whole life: the contract reserved it for a *lying* stream and named the innovation gate as its producer, which E-105 measured as unable | as E-113 | 11 Aug |
| E-115 | Zero false alarms, and bit-identity preserved | The clean redundant arm holds the integrity counter at **0** across 400 ticks and never leaves NOMINAL. A run with redundancy **off** is unchanged: the channels draw from a generator seeded `seed ^ 0x2ED0`, so they consume no draw the IMU would have taken — checked by the integration suite passing unchanged rather than asserted | `pytest tests/integration` | 11 Aug |
| E-116 | **The escalation is now wrong, and ADR-0024 predicted it** | With one of three channels faulted the vehicle drives *well* — 0.042 m, two good channels, a working median — and the integrity counter runs to **40 and HALTs it**. The counter asks *"is any modality unhealthy?"*, which was right with no redundancy to fall back on and is wrong now. **ADR-0024 named this exact trigger**: *"any modality, not a quorum ... when redundancy exists this is the line that should become a vote, and it needs its own decision record when it does."* Left as a named successor rather than folded in, because a fail-safe escalation policy should not change in the same commit that gives it a new input | as E-113 | 11 Aug |
| E-117 | **A tolerated fault no longer stops a vehicle that is driving well** | With `integrity_tolerated_faults = 1` and one of three channels FAULTED, the machine holds **NOMINAL with the integrity counter at 0** over 60 ticks — against **HALT at counter 40** under the previous rule. The vehicle in that state is at 0.042 m deviation on two good channels with a working median (E-113); stopping it was a recoverable single-channel fault converted into a terminal one | `pytest tests/unit/test_sensor_integrity.py` (23 tests) | 11 Aug |
| E-118 | **Losing the quorum still halts, and tolerating one is not tolerating any** | Same tolerance of 1, **two** of three channels FAULTED: **HALT at counter 40**, unchanged. The two results together are what makes the first safe — a change that only relaxed would be a fail-open mode wearing a redundancy argument | as E-117 | 11 Aug |
| E-119 | **At tolerance zero the change is bit-identical, and the whole suite says so** | One unhealthy channel already exceeds zero, so the arithmetic collapses to the previous *"any modality"* rule. Every shipped profile sets zero and **all 2,915 tests pass unchanged**, including ADR-0024's dropout escalation at +5 / +15 / +40 (E-87). The claim is checked rather than argued | `make check` | 11 Aug |
| E-120 | **OD-16: the integrity counter was never written to the log** | ADR-0024 gave L8 two counters and argued they must be reported separately — *"one integer cannot say which happened"* — and the field went onto `FailSafeSnapshot` and **not onto the audit record**. So every archive between schema 6 and 8 carries that argument's conclusion and none of its evidence: a DEGRADED posture with no way to tell whether the gates refused or a sensor went dark. **Fourth instance of one shape in five schema versions**, after `fast_innovation` (v3), `previous_digest` (v5) and the arbitration signature (v7). Closed at schema **7 → 8** | `astra explain <log> <tick>` on a pre-fix archive | 11 Aug |
| E-121 | **`astra explain` reconstructs one tick's decision from the archive alone** | Sensors, estimate, per-gate verdict with its evidence, posture with **which counter caused it**, arbitration with the context signature, and the issued command with why it differs from the proposal. On the IMU-dropout run at t250 it reports `fail-safe HALT / ood counter 0 / integrity counter 40` and the line **"escalated on SENSOR HEALTH, not on any veto"** — which is the sentence ADR-0024's whole argument rests on and which the archive could not support until E-120 was fixed. This is A-10's definition of explainability given a reader | `uv run astra explain var/explain-run 250` | 11 Aug |
| E-122 | It refuses to infer, and the refusal caught its own first bug | An absent field reads as **not recorded**, never as a plausible default. The first version read a missing `integrity_counter` as zero and therefore reported *"both counters equal"* on a HALT caused entirely by sensor health — an inference dressed as a finding. It now says which counter escalated **cannot be told** from a pre-schema-8 archive, and names why. Same rule as the dashboard's, and for the same reason (E-79): the most alarming thing an explanation can do is sound complete | as E-121 | 11 Aug |
| E-123 | **OD-17: the 95% coverage gate is an aggregate, and a module shipped at 10%** | `astra explain` — a forensic tool a safety case would lean on — landed with **no tests at all**, at **10.3%** coverage, and `make check` was **green**. 94 uncovered statements against a codebase of several thousand moved the aggregate by less than a tenth of a point. The gate said *"95% coverage"* and meant *"95% on average, and nothing about the thing you just wrote"* | `pytest --cov=astra.observability.explain` on the shipping commit | 11 Aug |
| E-124 | The floor fires on exactly the case that created it | `make coverage-floor` fails any file below **80%**, excluding `__main__.py` by name rather than by pattern. Replayed against `explain.py` at its shipped 10.3%: **exit code 1**, the file named. Against the tree today: **every file at or above 80%**, so it costs nothing to keep. The floor sits far *below* the 95% aggregate deliberately — its job is to catch a module with **no** tests, not to chase the last branches out of covered code, and a floor near the aggregate would fail three healthy files and be switched off within a week | `make coverage-floor` | 11 Aug |
| E-125 | The explainer's own coverage, after | **10.3% → 95.2%**, 29 tests. Most assert **absence**: that a missing field stays missing, because the positive cases are easy and would pass on a module that also invents things. One test is the module's own first bug — an absent `integrity_counter` read as zero, reporting *"both counters equal"* on a HALT caused entirely by sensor health | `pytest tests/unit/test_explain.py` | 11 Aug |
| E-126 | **OD-18: a camera failure stopped the vehicle exactly as an IMU failure did** | Driving the fail-safe machine with one channel absent for two seconds: **IMU → HALT, CAMERA → HALT, RADAR → HALT.** The counter summed unhealthy modalities and never asked which. The extractor calls `sample_for(SensorModality.IMU)` and nothing else, so in the shipped configuration the camera and radar contribute **nothing** to the state estimate all three gates read — and losing either stops the car. **A nuisance stop from a component that was not contributing** | `pytest tests/unit/test_sensor_integrity.py` | 11 Aug |
| E-127 | Declaring criticality removes the nuisance stop and keeps the real one | With `critical_modalities = [IMU]`: a camera failure holds **NOMINAL with the counter at 0**; an IMU failure still **HALTs**. Both asserted, because a change that only relaxed would be a fail-safe that had stopped escalating on sensor health at all — far worse than the defect it removes. An **empty** set is refused by a validator: it would silently disable the counter, a fail-open mode reachable by deleting one line of TOML | as E-126 | 11 Aug |
| E-128 | Bit-identical by default, and the prototype declines its own escape hatch | **Every shipped profile lists all five modalities**, which is exactly the behaviour before the field existed, and **all 2,953 tests pass unchanged**. Declaring only the IMU critical would be true *of this build* — and true only because of **OD-15**, five modalities carrying one measurement. Encoding a known defect as a deliberate safety claim would make the configuration a record of what is broken rather than of what is intended | `make check` | 11 Aug |
| E-129 | After ADR-0028 the vehicle had exactly **two** responses to a sensor failure | Driving the machine directly, one modality dark for 3 s: every **critical** modality reached `HALT` with the counter at 40, and every **non-critical** one changed nothing. `critical_modalities` is a switch, not a dial — it can say *this sensor does not matter* and cannot say *this sensor matters this much*. The response with the clearest per-sensor meaning — *lose the camera, stop changing lanes, keep driving* — was **unrepresentable**, because every effect the machine has reads `state`, and `state` comes from one integer | `python -m benchmarks.degradation` | 15 Aug |
| E-130 | Camera health is real **today**, which is what made the fix land now | L1 computes `StreamHealth` from per-stream **freshness**, whether or not anything consumes the payload — which is exactly how a dark camera drove the counter to `HALT` in E-126, on a build whose extractor reads the IMU alone. So a dependency map is a statement about *what the vehicle may do when a sensor is unhealthy*, not about what the estimator reads. **OD-15 blocks the estimation benefit and does not touch the capability benefit**; my first recommendation merged the two and was wrong | as E-129 | 15 Aug |
| E-131 | The vehicle now keeps driving and declines only what it can no longer do | With `critical_modalities = [IMU, GPS]` on the simulation profile's capability map, three of five modalities hold **`NOMINAL` with the integrity counter at 0** and withdraw only what they carry: camera → `lane_change, lane_keeping`; lidar → `obstacle_avoidance`; radar → `adaptive_cruise, lane_change, obstacle_avoidance`. IMU and GPS still `HALT`. **No row of the two-response design could look like this** | as E-129 | 15 Aug |
| E-132 | The degradation table is derived from the machine, and flags inert sensors | `benchmarks/degradation.py` drives the real fail-safe machine once per modality rather than restating the configuration, so the safety case's degradation concept and the running system **cannot drift apart**. A modality that is neither critical nor required by any capability is flagged **`INERT`** — losing it moves no posture and withdraws no function, which is the "added a sensor, forgot to wire its failure response" integration bug made visible. Inertness is **measured, not deduced**: it asks the machine what it did, so a regressed derivation shows up instead of being hidden | as E-129 | 15 Aug |
| ~~E-139~~ | **RETRACTED — measured on a stationary vehicle, see E-143.** **E-107 is too strong: the innovation *sequence* does separate the slow drift** | E-53 and E-105 both tested innovation **magnitude** — *"every step is well inside what the filter expects"* — and neither tested whether the sequence is **white**. It is not. Over the fault window the signed `position_y` residual carries a mean of **+0.242σ** against the clean run's **−0.003σ**, with the dominant sign on 60% of ticks against 53%. Every sample stays inside its band, and the *signs stop being a fair coin* | `python -m benchmarks.whiteness` | 15 Aug |
| ~~E-140~~ | **RETRACTED — measured on a stationary vehicle, see E-143.** The separation needs the right slack, and a sweep says which | At the textbook `k = 0.5` the CUSUM absorbs the whole 0.242σ offset and reports **1.21x** — which is how this nearly became a fifth refutation. Swept: **0.50 → 1.21x, 0.30 → 1.46x, 0.25 → 1.72x, 0.20 → 3.70x, 0.15 → 5.65x, 0.10 → 7.35x, 0.02 → 9.50x.** Separation begins where `k` drops below the fault's own mean offset, which is the expected place and not a tuned coincidence. **3.0x is the bar E-94 could not reach on any window**, quoted so both are judged the same way | as E-139 | 15 Aug |
| ~~E-141~~ | **RETRACTED — measured on a stationary vehicle, see E-143.** The false alarm surface, measured before any threshold was believed | E-96's warning applied to this candidate too: the plant starts up to 1 m off centre and corrects, *a sustained one-sided manoeuvre — the same shape a fault response has*. Measured from tick 0 rather than from cruise, the clean peak inflates **1.64x to 2.44x** — at `k = 0.10`, **8.14** against a cruise-only 4.46. The drift reaches **32.75**, so a threshold in **(8.14, 32.75)** detects it and never fires on a clean launch: a usable band with roughly 4x margin. **This is one seed on one trajectory and is therefore not a false-alarm rate** | as E-139 | 15 Aug |
| ~~E-142~~ | **RETRACTED — measured on a stationary vehicle, see E-143.** Not wired, and what would have to be true first | The detector is a **shadow measurement**. Three things block wiring, none of them cosmetic: it ran against the **placeholder proposer** because no policy checkpoint exists in the tree, so it is not comparable with E-53/E-105 and must be repeated against the trained one; `k` was chosen knowing the fault's magnitude, and a deployment would have to derive it from the *clean* run's own bias; and the **signed residual reaches no archive** — only its norm does, and a norm is non-negative by construction, so every run ever recorded has discarded the sign information this test needs | as E-139 | 15 Aug |
| E-143 | **The whiteness candidate is refuted too, and E-107 gains a fifth confirmation** | Re-run against `var/policy/jerkscaled.pt`, which drives at **11.40 m/s with 3 vetoes in 400 ticks**: the drift separates **1.03x–1.04x at every slack from 0.50 to 0.02**, while the bias separates **12.82x–14.26x** in the same sweep — so the statistic works and the drift is invisible to it. E-139 – E-142 reported 7.35x because they ran on the **placeholder proposer with 400/400 ticks vetoed and a final speed of zero**, a configuration in which E-107's mechanism — *the proposer closing the loop on a corrupted estimate and driving the vehicle toward it* — does not exist. **The false signal was the mechanism's absence**, which makes this stronger evidence for E-107 than a plain null: it shows the persistent innovation bias appearing exactly when the loop is opened, and vanishing when it is closed | `python -m benchmarks.whiteness --sweep --policy var/policy/jerkscaled.pt` | 15 Aug |
| ~~E-144~~ | **RETRACTED — asserted from a truncated listing, see E-145.** Claimed `var/policy/synthetic.pt` does not exist. It does |
| E-145 | What was actually wrong with the artefact path, after checking properly | **`var/policy/synthetic.pt` exists and drives** — E-144 said otherwise, from an `ls` of three directories piped through `head -20` where the cut landed one line above it. `make artifacts-check` now reports *"twin, corpus and policy present; the vehicle drives"*, and `python -m benchmarks.whiteness --sweep` reproduces E-143 with no flags. **The whiteness failure was caused entirely by a default path this session invented** (`artifacts/policy/policy.json`), not by a missing artefact. What remains true and was worth fixing: nothing **regenerated** the artefacts, nothing **verified** them, the commands lived only in a status document from 31 July, and none of the outputs are tracked — so a *clean checkout* still has no artefacts even though this working tree always did | `make artifacts-check`, `ls -la var/policy/` | 15 Aug |
| E-146 | Two wrong conclusions in one afternoon, both from incomplete observations | E-139 – E-142 concluded a detector worked, from a run on a vehicle that never moved. E-144 concluded an artefact was missing, from a listing that was truncated one line above it. **Both were assembled correctly from an invalid observation, and both passed every check that examines the assembly.** The shape is the register's oldest — OD-2 and OD-7 were inversions of the same kind — and the countermeasure is the same one that caught both here: distrust a result that arrives convenient, and go and look at the raw thing. `tools/check_artifacts.py` and `StationaryVehicleError` are that discipline made mechanical for the two specific cases | — | 15 Aug |
| E-162 | **Two of the three gates never object, on a suite built to break them** | Census across **2,800 ticks**, the clean arm plus all six faults, on the 15 August baseline: `STATISTICAL` **PASS 2800 / VETO 0**, `DETERMINISTIC` **PASS 2800 / VETO 0**, `PHYSICAL` PASS 2651 / VETO **149** — every one of them the same reason code, `LATERAL_JERK_EXCEEDS_LIMIT`. **One gate does all the work, on one reason** | `python -m benchmarks.gate_census` | 15 Aug |
| E-163 | The silent gates are **passing**, not abstaining, and the distinction is the finding | `ABSTAIN` is 0 for all three. ADR-0016 added abstention for a gate that *cannot* judge — uncalibrated, out of its competence — and silence of that kind is honest, with calibration as the remedy. These two **judge every tick and find nothing to object to**. Counting only vetoes cannot tell the two apart, which is why the census separates them | as E-162 | 15 Aug |
| E-164 | L6's silence has a known cause, and it is OD-8 | The statistical gate cannot veto because its live scores sit **entirely below** the corpus quantile it is compared against (E-159): 0% overlap, live 3.3648–3.4083 against a corpus of 3.8758–5.4312. So `VETO 0` is not evidence the proposals were sound — it is the exchangeability violation, seen from the gate's side. **The two open rows explain each other**, and neither is visible from the veto rate alone | as E-159, E-162 | 15 Aug |
| E-159 | **OD-8 survives the regeneration, and the gap widened** | Re-measured against a corpus rebuilt through *both* the corrected innovation covariance (ADR-0032) and redundant driven sensing (ADR-0033) — everything the score is built from moved. `URBAN_CLEAR`, **999 live samples against 1,000 calibration samples: zero overlap.** Live spans **3.3648–3.4083**, the corpus **3.8758–5.4312**. Still *below* the corpus, the same direction as E-41, and proportionally further: the live median was 0.2% under the corpus minimum in August and is now **13%** under. The conformal guarantee does not hold, so the veto rate measures this mismatch rather than the gate's discrimination | `python -m benchmarks.exchangeability` | 15 Aug |
| E-160 | The measurement is now a benchmark rather than two stale artefacts | E-41 was reproduced by reading `var/calibration/synthetic.json` beside `mean_live_score` in a soak file from 6 August. `benchmarks/exchangeability.py` drives the loop and recomputes the score with **the same public function the gate calls** — a reimplementation would have measured the reimplementation. It reports per context, so a class with too few live samples is visible as such rather than averaged away | as E-159 | 15 Aug |
| E-161 | The first version of that benchmark reported `100% inside` from **one** sample | Beside a genuine `0.0%` from 999, in the same column, both looking like fractions. One was a measurement and the other a coincidence with a percent sign on it. `render` now prints `n=1` and *"too few to judge"*, and the verdict paragraph ignores thin contexts rather than counting them either way. **Third instance today of the same shape** — a number assembled correctly from an observation nobody checked was adequate, after E-143 and E-145 | `pytest tests/unit/test_exchangeability.py` | 15 Aug |
| E-156 | **OD-11 walls 1 and 2 are down: the composition root takes a platform** | `assemble_pipeline` accepts keyword-only `space` and `projector`, defaulted to the automotive implementations. Two **strict** xfails had pinned this since the NFR5 audit, and strict is what made the fix announce itself — both flipped to `XPASS(strict)`, reported as failures, the moment the parameters existed. Suite goes **5 strict xfails to 3**. Defaulted rather than required on purpose: this root *is* automotive until a second platform exists, and NFR5 asks that a different one **can** supply one | `pytest tests/architecture/test_domain_independence.py` | 15 Aug |
| E-157 | Writing an honest injection test found three couplings review had not named | The old assertions were `any("space" in name for name in parameters)` — satisfied by a parameter called `workspace`, and by one accepted and ignored. Driving the root with a real two-channel differential drive instead surfaced: the **projector** (which the xfail did describe), the **placeholder policy** (which it did not — it takes `speed_index` and `steer_index` into the space), and **`twin.control_effectiveness`**, whose length *is* the channel count. **Making the space injectable is not the same as making the root platform-neutral, and only driving it with a different space says which** | as E-156 | 15 Aug |
| E-158 | The configuration layer already caught the third, better than a test could | `twin.control_effectiveness has 3 entries but the actuation space has 2 channels ('left_wheel', 'right_wheel'); the row maps a command to the lateral acceleration it produces, so a length mismatch describes a different platform`. **Not an NFR5 violation** — a different platform needing a different profile is correct — but it means an adapter supplies **four** coordinated things, and nothing had said so. The root now refuses a supplied space without a projector and a policy, at the point where it can be acted on rather than as an index error deep in construction | as E-156 | 15 Aug |
| E-152 | **OD-15 closed: the vehicle is driven by three channels, and the clean run got six times better** | `drive_closed_loop` now builds three independent position channels — IMU σ=0.1, GPS σ=0.2, LIDAR σ=0.06, **deliberately unequal** because identical sigmas model identical sensors and identical sensors share a failure mode — unless a caller passes `single_channel=True` and says so. Measured, same seed: **5 vetoes → 1**, final deviation **0.1034 m → 0.0168 m**, speed 10.85 → 12.09 m/s. **This more than repays ADR-0032's cost**: the honest fix for a filter that must trust each measurement less is better measurements, not retuning | `python -m benchmarks.arms` **(corrected 17 Aug 2026 — this row cited `benchmarks.redundancy`, which prints the shadow residual monitor and none of these figures)** | 15 Aug |
| E-153 | **A 1 m bias in one channel no longer reaches the estimator at all** | The best result of the redundancy work. Peak estimator error under a 1.0 m IMU bias: **1.1805 m single-channel → 0.1323 m redundant**; final deviation **0.8387 m → 0.0168 m**. That second figure is the **clean run's, to four decimals** — under redundancy the biased arm and the healthy arm are indistinguishable, which is what outvoting a liar looks like when it works. The test that asserted the bias *arrives* has been inverted | `python -m benchmarks.arms` for the figures; `pytest tests/integration/test_closed_loop_faults.py` for the assertion that guards them **(corrected 17 Aug 2026 — the test asserts the property, `biased_mean - clean_mean == approx(0, abs=0.1)`, and prints no figure; 1.1805 / 0.1323 appear in it only as a comment)** | 15 Aug |
| E-154 | The dropout's verdict trace now differs — and it is **not** a gate detecting the fault | Under three channels a dark IMU produces **17 vetoes** in the fault window against the clean run's 0, first at tick 229. Every one is `LATERAL_JERK_EXCEEDS_LIMIT`: the jerk bound reacting to the control effort that follows losing a channel, **not a gate identifying a sensor as dishonest**. The integrity counter still is the only thing that identifies it, and the ratio that tells a reader which happened is now **40 to 3** rather than 40 to 0. The claim is narrower than *"Core-B can see it"* and is asserted as the narrower one | as E-153 | 15 Aug |
| E-155 | **Retraining the policy changes nothing, because the policy never sees the filter** | `training/train_policy.py` trains against `SyntheticDrivingEnv` directly — the bare plant, no pipeline, no UKF, no sensor bus. Retrained after ADR-0032 and again after ADR-0033, it produced a **bit-identical checkpoint each time**, `sha256:ac5b91bc…`. So it cannot be mismatched to the filter, and ADR-0032's claim that it was retrained *to match* is corrected. The larger fact this exposes: **the proposer is trained on ground truth and deployed against an estimate**, and no register row covers that skew | `sha256sum var/policy/synthetic.pt` | 15 Aug |
| E-147 | **OD-10 closed: the innovation covariance now carries `H Q Hᵀ`** | `predict` redraws the sigma points from the `Q`-inflated covariance, so the update observes a set whose spread is the full predicted covariance. Measured over 400 closed-loop ticks, old against new: p25 **1.39x**, median **1.34x**, p95 1.07x, max **1.02x**. E-71 predicted 1.024x on the largest single distance from a different measurement and this lands on **1.02x**. The correction is largest where innovations are small and **nearly absent in the tail**, which is where vetoes are decided — the reason OD-10 was rightly downgraded from its 22.4x algebraic bound | `python -m benchmarks.fault_study` | 15 Aug |
| E-148 | The fix would not drive until the corpus was regenerated, and the new guard caught it | With the corrected filter and the **old** corpus, **400 of 400 ticks vetoed** on `SCORE_EXCEEDS_CONFORMAL_QUANTILE` — the corpus had been calibrated on the old statistic. `make artifacts-check`, added hours earlier the same day, failed immediately with *"the vehicle never drove"*. **Its first real regression, and the reason it exists.** Corpus regenerated, policy retrained: mean lane deviation **0.0913 m**, all three constraints satisfied | `make artifacts-check`, `make artifacts` | 15 Aug |
| E-149 | The honest cost: a correct filter converges more slowly | A larger `S` means a smaller Kalman gain, so each measurement is trusted less. Final lane deviation **0.0122 m → 0.1218 m**, and the clean-run figure in the fault study 0.1034 m against an old bound of 0.1. Both test bounds re-derived from the corrected system **with the old numbers recorded beside them**; the vehicle stays far inside the policy's own 0.875 m budget and the 1.75 m corridor. Loosening a safety-adjacent bound is the move that most needs to stay visible | `pytest tests/integration/` | 15 Aug |
| E-150 | **The frozen IMU now makes the system look *healthier* than the healthy run** | Two integration tests failed in ways that were not threshold drift, and both sharpen OD-9. **Vetoes:** clean has 5, three of them after the fault opens (ticks 230, 300, 368); the faulted run has 2 and **none** after. A frozen IMU stops the estimate moving, so commands get smoother and the jerk bound is never reached. **Trajectory:** the faulted run also ends *closer* to centre (0.0973 m against 0.1034 m) with a *smaller* estimator error (0.0974 m against 0.1547 m), because a dropout makes the filter coast. The old test asserted the faulted run was worse by trajectory; that was reading a coincidence and is deleted | as E-147 | 15 Aug |
| E-151 | Only the integrity counter separates the two runs — 40 against 0 | Having removed the two proxies that happened to correlate, **nothing in the trajectory, the estimator error or the verdict trace distinguishes a frozen IMU from a healthy run.** The sensor-integrity counter does, and it is now the *only* discriminator rather than one of several. That is precisely the claim [ADR-0024](adr/0024-sensor-integrity-is-a-second-counter-not-a-fourth-gate.md) was built to support, and OD-9's argument for a signal upstream of the common cause is stronger for the other candidates having fallen away | as E-147 | 15 Aug |
| E-134 | Four health levels, two responses | Holding a camera at each level for 3 s on the shipped profile: `DEGRADED`, `FAULTED` and `ABSENT` **all reach HALT with the counter at 40**, indistinguishable in posture, counter and withdrawal. Only L9's context signature grades them, weighting 1.0 / 0.5 / 0.1 / 0.0, and that feeds profile matching rather than the fail-safe. **A camera arriving late stopped the vehicle exactly as a camera that was gone** — and `StreamHealth`'s own docstring had said since Phase 1 that collapsing the two *"loses that distinction"* | `python -m benchmarks.degradation` | 15 Aug |
| E-135 | An intermittently failing sensor is invisible, however long it fails | The integrity counter moves +1 unhealthy / −1 healthy, so **any duty cycle at or below 50% nets to zero**. Over 1,200 ticks (60 s at 20 Hz) with a camera failing on a cycle: **1 dark / 1 clean → 600 ticks dark, peak φ = 1, NOMINAL**; 3/3 → 600 dark, peak 3, NOMINAL; 3 dark / 10 clean → 279 dark (23%), peak 3, NOMINAL. 20/20 → LIMP, and only because the *period* exceeded the threshold, not because the fault was worse. **It is a duty-cycle detector with a 50% threshold, described as a health detector** | as E-134 | 15 Aug |
| E-136 | The kind of fault now decides how far the posture goes | With `integrity_ceiling = {DEGRADED = LIMP, FAULTED = HALT, ABSENT = HALT}`: a stale camera settles at **LIMP** — slowed, still driving — while a faulted or dark one still **HALTs**. The counter reads 40 in all three, so the cap changes the posture and hides nothing from the log. The **recovery** case is asserted separately: a sensor coming back must not lift the cap and halt the vehicle on the counter it already built, which is why the ceiling is a high-water mark | as E-134 | 15 Aug |
| E-137 | Decay reports the duty cycle the counter cancels | Same six patterns as E-135, after ADR-0031: 1/1 → **0.497**, 3/3 → 0.492, 10/10 → 0.475, 3/10 → **0.240** against a true 0.231. The counter still peaks at 1 on the first, **correctly** — it answers *am I in trouble now* — and the decay answers *is this sensor dying*. Per modality, so a dying camera does not make the IMU look ill; the counter is aggregate and cannot attribute | as E-134 | 15 Aug |
| E-138 | The service signal is derived, attributable, and drives nothing | With `decay_service_threshold = 0.20`, a camera at a 23% duty cycle is named in `sensors_needing_service` while the posture stays **NOMINAL**, the counter at ≤ 1 and the speed cap `None`. **No shipped profile sets the threshold**: what fraction of dropped frames means *service this* is a property of a particular sensor on a particular vehicle and this project has measured no such number. Not cleared by `reset`, so halt-reset-halt cannot launder a failing sensor clean | as E-134 | 15 Aug |
| E-133 | Provably additive: the two axes are independent and the default is unchanged | All **2,958 pre-existing tests pass untouched**, plus 61 new (21 withdrawal, 7 table, 13 ceiling, 20 decay) — 3,019 total, `mypy --strict` over 162 files, per-file coverage floor green at 97.56%. Both directions of independence asserted: `NOMINAL` with functions withdrawn, and every function intact while the posture escalates on refusal. An empty `capabilities` declaration reproduces the previous behaviour exactly, which is why the field may default to empty without bending A-4 — an empty `critical_modalities` disables a counter that already escalates and is a fail-open *claim*; this is the absence of a claim | `make check` | 15 Aug |

† **Historical, not reproducible from the current tree.** These three measured the
elastic-weight-consolidation penalty, which [ADR-0019](adr/0019-one-twin-head-per-context.md)
deleted along with `ewc_lambda`. They are kept because they are the evidence *for*
that decision, and a table that silently dropped the measurements behind its own
changes would be a worse record than one carrying a footnote. The behaviour E-30
describes is still reproducible as the control arm of E-33.

### Controlled comparisons

| # | Question | Result | Date |
|:--:|---|---|:--:|
| E-25 | Does removing the OOD counter's ceiling get caught? | **Yes.** Deleting the `min` fails exactly the two tests that name it — `test_the_counter_never_climbs_past_the_halt_threshold` and `test_the_ceiling_is_the_halt_threshold_and_not_some_multiple_of_it` — and the other 2,641 pass | 6 Aug |
| E-31 | Would rescaling the EWC penalty work instead of re-anchoring it? | **No, measured.** Normalising the Fisher by its mean shifts the useful window from 1e6 to 1e4 without widening it -- still one decade, still a cliff at 10x -- and the best protection available drops from **0.084** to **0.309** of unregularised. Strictly worse than tuning alone | 6 Aug |
| E-32 | Does EWC protect the old context selectively, or just slow all learning? | **Just slows everything.** Forgetting divided by fraction-of-gap-closed, across lambda 0 to 1e5: **0.00184, 0.00184, 0.00186, 0.00186, 0.00194** -- constant. Structural: a 16->2 linear readout gives a Fisher-weighted penalty no disjoint subspace to exploit. Open as P3.1a, held as a strict xfail | 6 Aug |
| E-35 | Interference when one head serves two contexts | **0.85 units of highway accuracy lost per unit of rain gained.** With a head each, 0.00 | 6 Aug |
| E-19 | Does the action-rate penalty need restricting to steering, or just more training? | **Both.** 2×2 at fixed seed: 98k/all-channels stops (return 549); 786k/all-channels stops (671); 98k/steer-only stops; 786k/steer-only holds 13.0 m/s (986) | 5 Aug |
| E-20 | Does regenerating the corpus from the deployed policy matter? | **Yes.** HIGHWAY_CLEAR quantile **1.18 → 2.43**; the threshold in production was less than half what the deployed proposer routinely produces | 5 Aug |
| E-21 | Is the veto latch a property of the policy? | **No.** Three policies — one that stopped the car, one holding 13 m/s at 0.03 m, one with an explicit jerk penalty — all reached the identical terminal state | 5 Aug |

---

## Not demonstrated

Listed because an evidence pack without this section is a sales document. Nothing
below is claimed anywhere in the repository as measured; where a document does
claim it, that is a defect and is named.

| # | Not demonstrated | Why not, and what would demonstrate it |
|:--:|---|---|
| N-1 | **Any false-positive or false-negative rate for any gate** | The plant, twin and corpus share one set of equations. Requires Phase 7 (CARLA) |
| N-2 | **Gate independence** — that three structurally different failure modes exist | Requires the validation drive where FGSM fires exactly one gate and IMU corruption fires two for different reasons. **This row moved in the wrong direction on 9 August and the movement is measured, not argued.** L6 and L7b both score proposal-against-twin, which was already a visible common cause; E-48 adds a second and larger one — *every* Core-B gate reads L2's fast estimate, and a sensor fault the estimator absorbs is therefore invisible to all three at once. Under an injected IMU dropout the vehicle spent 73 ticks outside its corridor while the bound that exists to catch exactly that read 0.023 m. The remaining question is no longer whether independence has been demonstrated; it is whether it is true |
| N-3 | **The 1.25 µs Core-B intercept** | An analytical AbsInt aiT bound for RTL that does not exist. Not measurable by a Python prototype and must never be quoted as measured |
| N-4 | **ASIL-D(D)** | A design target. An ASIL is the outcome of an assessed safety case |
| N-5 | **Coverage on real driving** | E-12 shows the quantile arithmetic is right, against a corpus drawn from a world the twin already models |
| ~~N-6~~ | ~~**Domain independence (NFR5, assumption A-1)**~~ | **Tested 10 August 2026, and it is partly false.** A differential-drive warehouse AGV meets four walls: the actuation space and command projector are not injectable, L2's process model is a bicycle model that cannot represent turning on the spot, and the kernel names road friction, tyre wear and highways (E-72 – E-75). **The claim holds for the gates and fails for the plumbing.** Each wall is pinned as a strict xfail, so making the claim true turns the suite red and forces this row to be rewritten |
| N-14 | **A false-positive rate on real driving** | E-42 dissolves the *internal* contradiction -- per-tick and per-intervention are different rates, and the intervention rate is 0.008% at the design point -- but it is **[M-syn]**. The plant, twin and corpus share one set of equations, so this shows the two numbers are compatible, **not** that either holds on a real vehicle. Only comma2k19 does that |
| N-7 | **Two of four feedback loops** | FB1 is wired. **FB4 is too** -- `training/closed_loop.py` steps the plant with whatever L9 issued, which is what makes a veto change the trajectory; it lives in the harness rather than `src/` because it is prototype-only and has no deployment counterpart. FB2 and FB3 have no callers, and both were measured in shadow and found to break the gate they feed (E-39, E-40). This row previously said three of four were unwired, which was wrong about FB4 |
| N-8 | **The fail-safe machine responds to a sensor fault at all** | The enforcement half closed with P2.1 (E-24): driven into HALT, the cap reaches an actuator. The *escalation* half now has an injector and an answer, and the answer is that it never escalates — across six injected faults the machine stayed **NOMINAL on all 400 ticks of every run**, including the one that put the vehicle 4.199 m off a 1.75 m lane (E-46). Nothing produced the vetoes the OOD counter needs, so nothing reached θ₁. This row is reopened rather than closed: what was undemonstrated is now measured as absent |
| N-9 | **The deterministic shield's contribution** | L7a vetoed **once** in ~500,000 ticks. It is a state monitor and cannot see a lane departure. The corridor bound added by P2.1 gives it one that it can, but no run has yet fired it |
| ~~N-10~~ | ~~**Tamper-evidence of the evidence log**~~ | **Closed 10 August 2026** — the log is a hash chain and alteration, deletion, insertion and reordering are all detected (E-66). A threat model now exists ([`THREAT_MODEL.md`](THREAT_MODEL.md)). What remains open and is now named precisely rather than lumped together: **tail truncation** and **a consistent whole-file rewrite** are undetectable by chaining alone (E-67), and there are still **no signed artefacts and no key management**, which is what would close both |
| N-11 | **Anything about a real vehicle** | The UKF has met only synthetic dynamics. Technical-debt item 1 |
| N-12 | **The paper's §5 validation drive** | 21 minutes, seven phases, 47 evidence tuples — never run. Recorded in `WORK_PLAN.md` §1.1 |

---

## Claims in the repository that this table contradicts

Feeding [`PENDING.md`](PENDING.md) P1.4, documentation sync.

| Where | Claim | Status |
|---|---|---|
| `README.md` | "2 513 tests, 97.97% coverage" | **Corrected 5 Aug** to 2,675 / 97.99%, and again **9 Aug** to 2,729 / 97.99% over 146 files (E-1) |
| `README.md` | "Full ten-layer tick p99 **1.98 ms**" | **Corrected 2 Aug.** The soak measures **9.3 ms** p99 for the full tick; `benchmarks/latency.py` measures a *subset* (L1+L2+L7a+L8) at 0.811 ms (E-10). Neither was "the full ten-layer tick" at 1.98 ms, and the two must not be compared |
| `README.md` | "Closed-loop over 400 ticks: trained policy 41.0% veto rate and 0.383 m mean lane deviation vs 59.8% / 0.836 m" | **Corrected 2 Aug.** Measured with a policy that stopped the vehicle, an unobservable lateral position, a corpus harvested from a different proposer, and a plant integrating 2.5× fast. Superseded by E-4, E-5, E-13 |
| `docs/PROJECT_STATE_AND_ROADMAP.md` | Contract, certification-field and layer-status counts | **Open.** Not re-verified since 31 July |
| `docs/1144_2026-07-31_Sushanth_status.md` | Superseded by the 20:30 ledger | **Open.** Should be marked as such |
| `docs/COMMERCIAL_ASSESSMENT.md` | "2,513 tests at 98% coverage" (twice) | **Not a defect, and listed so nobody files it as one.** The document is a dated assessment — 31 July 2026 — and the figure was correct on that date. A dated snapshot that is silently refreshed stops being a snapshot. It should be re-issued rather than edited, and until it is, its date is the caveat |

---

## How to add a row

A row needs a **command that reproduces it on a clean checkout**. If the figure
came from a one-off script, either commit the script or do not add the row.
Prefer citing the artefact path (`var/soak/<name>/summary.json`) over a number
retyped by hand — retyping is where the last set of stale figures came from.
