# ASTRA — session handover

**Written** 19:18, 9 August 2026
**Head** `1236daf`
**Branch** `phase4-l5-twin-l7b-physical`
**Gate** green — 2,693 tests, 97.99 % coverage, `ruff` + `mypy --strict` over 143
files, 12 `lint-imports` contracts kept, 0 broken
**Tree** clean; `var/` artefacts committed and current

> **Check the dates before publishing anything.** Every row in `EVIDENCE.md` and
> every ADR from this run is stamped **2 August 2026**, carried forward from the
> convention already in the files. Today is the 9th. Either the work genuinely
> spans a week or the stamps were never advanced — I could not tell from inside
> the session, and a date that is wrong in an evidence pack is the same class of
> defect as a number that is wrong. Worth ten minutes with `git log --date=short`.

---

## 1 · Read these first, in this order

| Document | What it is |
|---|---|
| [`PENDING.md`](PENDING.md) | Every open item, priority-ordered, each with the options considered and why one was chosen. **The main artefact of this session.** |
| [`EVIDENCE.md`](EVIDENCE.md) | The log: one row per measurement, the command that reproduces it, the date |
| [`CREDIBILITY_MATRIX.md`](CREDIBILITY_MATRIX.md) | The judgement: one row per *claim*, its provenance marker, what it does not license |
| [`DATA_SPLIT_PROTOCOL.md`](DATA_SPLIT_PROTOCOL.md) | How external data must be partitioned before it produces any number |
| [`ENGAGEMENT_DELIVERABLES.md`](ENGAGEMENT_DELIVERABLES.md) | What is offered to a partner, and what must not be promised |
| `adr/0016`–`adr/0020` | The five decisions taken, with the rejected options |

**The division that keeps them honest:** `EVIDENCE.md` holds numbers,
`CREDIBILITY_MATRIX.md` cites them and never restates them. That rule exists
because the matrix spent a month carrying `1.98 ms`, `41.0 % vs 59.8 %` and
`2,513 tests` after all three had been superseded.

---

## 2 · Environment — read before running anything

The quality gate **does not run on the Windows host**. Smart App Control blocks
the unsigned native extensions in `torch`, `mypy` and `grimp`.

```bash
# WSL2 Ubuntu, default user `astra`, project at ~/astra
cd ~/astra
export PATH=$PWD/.venv/bin:$HOME/.local/bin:$PATH
make check RUN=          # RUN= because uv is not on the gate's PATH by default
```

Edits are made on the Windows side (`C:\Users\Dell\Documents\ASTRA`) and rsynced
in; results are rsynced back. Both directions are needed, and **forgetting the
way back is how work gets lost** — see §6.

```bash
rsync -rc --exclude=.venv --exclude=.git --exclude=var --exclude=__pycache__ \
  --exclude=.mypy_cache --exclude=.pytest_cache --exclude=.ruff_cache \
  /mnt/c/Users/Dell/Documents/ASTRA/ ~/astra/          # in
rsync -rc ~/astra/src/ /mnt/c/Users/Dell/Documents/ASTRA/src/            # back
```

The WSL default user changed from `root` to `astra` mid-session, which stranded
the old tree under `/root`. The environment has been rebuilt under `/home/astra`
and verified. Git identity is now `Sushanth C <sushanthc.cs23@bmsce.ac.in>`,
repo-local **and** global.

---

## 3 · What was done

Twenty-one commits, `e19d0c1` … `1236daf`. Grouped by what they achieved rather
than by order.

### Defects found and fixed

| | Was | Now |
|---|---|---|
| **Fail-safe speed cap** (OD-2) | Recorded on every capped tick, applied to no actuator. A run held 17.2 m/s **in HALT** while every audit row agreed it was capped | Projected onto the command last, so it binds on blocked and exploring ticks too. Throttle 0.0, brake 1.0, pinned by a test through the assembled pipeline |
| **OOD counter** (OD-5) | Unbounded, 1,508 by tick 2,000 | Clamped to `[0, θ_halt]`. Also lets the recovery bound be *stated*: ≤ 91 clean ticks, 4.6 s |
| **EWC anchor** | Re-taken every buffer flush, so the penalty was **bit-for-bit inert** — 0.038951 against 0.038972 unregularised | ADR-0018, then superseded entirely by ADR-0019 |
| **Twin forgetting** | One output layer for all contexts; adapting to rain destroyed the highway by 159× | ADR-0019: one head per `ContextClass`. Forgetting is now structurally impossible — the test asserts `==`, not a tolerance |
| **`train_twin.py`** | Fitted the trunk and one head, leaving the others at random init — a loaded twin would predict noise in every context but one | `seed_heads_from()` broadcasts. Caught by the forgetting test refusing to agree the offline twin knew the highway |
| **FB3's statistic** | `recalibrate()` wrote L6's non-conformity score into **L3's innovation distribution** — re-merging what P2.5 separated. Introduced by P2.5 itself | Each layer requantilises its own statistic; parameter renamed to say which |
| **FB4's no-command branch** | `[0.0, 1.0, 0.0]` in the plant's *normalised* space decodes to **half throttle** and full brake, in the one situation the branch exists for | `[-1.0, 1.0, 0.0]`, with a test that decodes through the plant's own bounds |
| **Stale `var/` artefacts** | The tracked corpus was schema v1 with no `innovations`; a clean checkout reproduced none of the day's numbers | Current artefacts committed and verified |

### Decisions recorded

- **ADR-0018** — the EWC anchor moves on a context change, not every update
- **ADR-0019** — one twin output head per context, replacing the penalty
- **ADR-0020** — FB2 estimates the control effectiveness instead of regressing on
  commands. Estimator built and tested (18 tests); **not yet wired or shadowed**

### Verification built

- `benchmarks/flake_hunt.py` — 220 runs, 220 passes, no hangs under
  `stress-ng --cpu 32`. Classifies **three** outcomes: pass, fail, **hang**
- `make lockfile` in the gate; `make verify-install` separate and
  mutation-verified
- The **shadow harness** — feedback loops run against state nothing reads,
  changing no verdict. Six tests pin the isolation; a 100k soak confirms the
  drive is numerically identical with it on

---

## 4 · The findings that matter

These are what a reviewer should be shown, and they are the reason the session
took as long as it did.

**1 · FB2 would disarm the statistical gate.** Its only training labels are the
proposer's own commands. Run in shadow over 100,000 ticks in a context where
nothing changed: the live non-conformity score stayed **flat to four decimal
places** while the shadow's fell **40 %**, monotonically, still falling at the
end. The twin's own module docstring names this as the way to disarm the gate.
`(E-38, E-39, OD-7)`

**2 · FB3 would turn the gate into a fixed-rate sampler.** Requantilising on
scores the system generates itself drives the veto rate to **5.02 %** — which is
`significance_epsilon` exactly, because ε of any distribution lies above its own
1−ε quantile. It fires at ε whether or not anything is wrong. `(E-40)`

**3 · D-1 was a units error, not a contradiction.** "False-positive rate < 1 %"
against ε = 0.05 looked incompatible. It never said *per what*. A veto runs the
fallback for one tick; nothing degrades until the OOD counter crosses θ₁ = 10.
Measured at the design point: **4.97 % of ticks vetoed, 0.008 % outside
NOMINAL** — two episodes in 83 minutes, both self-recovering, LIMP and HALT never
reached. ε stays at 0.05. `(E-42)`

**4 · The live loop is not exchangeable with its own corpus.** Running scores sit
at 1.156, **below the corpus minimum of 1.158**. Exchangeability is what the whole
conformal guarantee rests on, and it already fails — on synthetic data, before any
external dataset. Today's 0.089 % veto rate is that mismatch, not evidence the
gate discriminates. `(E-41, OD-8)`

**5 · The pattern.** Two of four feedback loops were broken, and it was not
random: **the two that feed a gate's own inputs were both broken; the two that
feed state estimation and the plant were not.** That says where to look next.

**6 · What caught all of it.** Not the 2,693-test suite, not `mypy --strict`, not
the 12 import contracts. Every one came from running the system for a long time
and reading the numbers, or from running a mechanism in shadow and diffing it
against the live one. Two were **inversions** — the evidence log confidently
recorded something that had not happened — and those are invisible to testing by
construction.

---

## 5 · What is open

### The one thing I would do next

**Build fault injection.** It is the same missing piece under five separate items:

- **P3.4** — ablating a layer on nominal driving where nothing goes wrong shows
  nothing. "L6 off" changes no outcome if no command was ever anomalous
- **P3.5** — the comparison harness, which `PENDING.md` calls *"the single most
  persuasive artefact available"*. Two instances, same seed, same **injected
  fault**. Explicitly the missing half
- **P4.2** — is fault injection
- **Section D of the matrix** — empty, and D-0 says why: nothing in the synthetic
  plant is out-of-distribution in the sense L6 is calibrated for. Not fixable
  with more ticks
- **P2.1's own limitation** — the speed cap's enforcement under a *fault* rather
  than a deliberate provocation

Everything measured so far shows the machinery **runs**. A fault injector is what
lets the project say what each gate **catches** — the actual product claim, and
the one currently at zero rows.

Build the injector *and its own tests first*. A fault injector nobody has verified
actually injects is worth nothing — same lesson as the flake harness, which
needed a test that really hangs.

### Decisions that are yours, not mine

- **P3.4's `AblationProfile`.** Three of six ablations need disable paths that do
  not exist: `statistical_gate` and `shield` are *required* constructor
  parameters, and exploration is an arbitration outcome rather than a switch.
  That requirement is load-bearing — a pipeline that could silently run without a
  gate is the worst defect this codebase could carry. My proposed shape is in
  `PENDING.md` P3.4; whether it is a strong enough guarantee to justify relaxing
  those parameters is a call I did not want to make alone.
- **Commit history.** Commits before `382de72` are authored `Your Tanay S`, after
  are `Sushanth C`, all with your email. Rewriting is possible **only until this
  is pushed**.

### Ordered backlog

| | Item | Estimate |
|---|---|---|
| 1 | Fault injection (unblocks the four below) | 1–2 d |
| 2 | **P3.5** comparison harness — ASTRA vs raw Core-A, same seed, same fault | 3–5 d |
| 3 | **ADR-0020** — shadow the effectiveness estimator, then the port change so L5 reads it | 2 d |
| 4 | **P3.4** ablation study, after the `AblationProfile` decision | 4–6 d |
| 5 | **P4.6** split-protocol enforcement — **binding**: the manifest and disjointness test land in the *same change* as the first ingestion script, or that script does not land | 0.5 d |
| 6 | **P4.1** dashboard · **P4.3** replace FilterPy · **P4.4** NFR5 domain test · **P4.5** threat model | parallel |
| 7 | **P5** GPU host, CARLA, the seven-phase validation drive — the only work producing non-self-referential validation | hardware-gated |

---

## 6 · Traps

Every one of these cost time this session.

1. **`git add -A -- docs` sweeps in untracked files.** It committed your three
   working documents twice. Name files explicitly.
2. **Backticks inside `python -c "…"`** get command-substituted by bash. It ate a
   class name. **Use `python - <<'PYEOF'` heredocs for anything with backticks.**
3. **`str.replace("", new)` inserts between every character.** A slice that
   searched for a section heading from the start of the file rather than from the
   section found an earlier one, produced an empty slice, and blew `PENDING.md`
   from 63 KB to **210 MB**. Always bound the second `index()` by the first, and
   check the byte count after.
4. **`var/` is gitignored but three artefacts are force-tracked.** The twin,
   corpus and policy. If you regenerate any of them, `git add -f` them back —
   otherwise the repo reproduces different numbers from the ones it documents.
5. **Assert before you conclude.** The ADR-0020 probe read the effectiveness
   17 % low and I nearly recorded that as a property of the estimator. It was a
   property of my probe — I had driven the plant past saturation. The estimator is
   exact.
6. **Regenerate the corpus after closing any feedback loop.** Closing FB1 alone
   moved the veto rate from 59.8 % to 99.8 % with no change to the policy.

---

## 7 · Standing conventions this session established

- **No feedback loop gets authority until it has run with none.** The shadow
  harness exists for this and has caught two defects no test would have.
- **Cite `EVIDENCE.md`, never restate it.** A number in two places will be wrong
  in one of them.
- **Reconcile the open-defect register in the same change that fixes a defect it
  names.** A register that overstates what is broken loses trust as fast as one
  that understates it.
- **Both figures or neither.** The false-positive rate is 5 % per tick and
  0.008 % per intervention. Quoting the second alone would be reporting the
  flattering half of a measurement.
- **Retract, do not refresh.** Superseded numbers stay visible with their
  correction, so the record shows they were wrong.
